# AGENTS.md

## Lue tämä ensin

Lue tämä tiedosto ennen jokaista tähän Unity-projektiin liittyvää tehtävää ja noudata sen ohjeita. Jos projektin alikansioissa on omia `AGENTS.md`-tiedostoja, noudata niitä kyseisen alikansion tiedostoissa tämän tiedoston lisäksi.

## Projekti

- Unity-versio: `6000.6.0f1`
- Projektin juuri: `C:\Users\marku\Documents\ChatGPT\Unity\My project`
- Unity CLI on käytettävissä projektikohtaisesti.
- Avoimen Editorin tila tarkistetaan komennolla `unity status`.

## Työskentelyohjeet

- Tee muutokset ensisijaisesti `Assets`-kansioon.
- Älä muokkaa `Library`-, `Temp`- tai `Logs`-kansioita käsin.
- Älä poista tai korvaa projektitiedostoja ilman käyttäjän nimenomaista pyyntöä.
- Säilytä nykyinen Unity-versio, ellei käyttäjä pyydä vaihtamaan sitä.
- Käytä Unity CLI:tä, kun tehtävä koskee avointa Unity Editoria, projektin tilaa, testejä tai buildausta.
- Tarkista muutosten jälkeen Unity-konsolin virheet ja aja asiaankuuluvat testit, jos se on turvallista.
- Ilmoita ennen laajoja tai mahdollisesti rikkovia muutoksia, mitä aiot muuttaa.

## Unity CLI

Kun Editor on auki, tarkista yhteys:

```powershell
unity status
```

Käytä projektin omaa Unity CLI -taitoa Unity Editorin ohjaamiseen. Jos CLI ei löydä Editoria, tarkista ensin, että oikea projekti on avattu ja että Pipeline-paketti on asennettu.

## Julkaisuprosessi

Peli jaetaan GitHubin ([Marcebooy/SurvivorPrototype](https://github.com/Marcebooy/SurvivorPrototype), public) kautta. Kaverin pysyvä latauslinkki launcherille: `https://github.com/Marcebooy/SurvivorPrototype/releases/download/launcher/SurvivorPrototypeLauncher.exe` (launcher itse tarkistaa/lataa uusimman pelin releasen automaattisesti).

Uutta versiota julkaistaessa:
1. Buildaa peli (Unity CLI, StandaloneWindows64), zippaa `Builds/`-kansioon.
2. `git add`/`commit`/`push` koodimuutokset.
3. `gh release create vX.Y.Z <zip> --title "vX.Y.Z" --notes "..."` — releasen kuvaustekstiin muutoslista.
4. **Discord-ilmoitus on lähetettävä erikseen käsin webhookilla** — GitHubin oma Discord-integraatio (poistettu 2026-09-08) näyttää vain otsikkorivin, ei koskaan release-tekstiä. Lähetä sen sijaan oma viesti suoraan webhookiin, esim.:
   ```bash
   WEBHOOK=$(cat ".secrets/discord-webhook.txt")
   curl -s -X POST "$WEBHOOK" -H "Content-Type: application/json" -d @- <<'EOF'
   {"content": "**Uusi päivitys julkaistu: vX.Y.Z**\nKäynnistä peli uudelleen — launcher lataa päivityksen automaattisesti.\n\nMuutokset:\n• ...\n• ..."}
   EOF
   ```
   Webhook-URL on tallennettu `.secrets/discord-webhook.txt`-tiedostoon (gitignoroitu — **älä koskaan committaa tätä URL:ia**, repo on public ja URL toimisi kenelle tahansa salasanana kanavalle kirjoittamiseen).

## Viestintä

- Vastaa käyttäjälle suomeksi, ellei hän pyydä muuta.
- Kerro lyhyesti, mitä muutettiin ja miten muutos varmennettiin.
- Älä väitä tehtävää valmiiksi ilman tarkistusta.
- Kirjaa tehdyt muutokset lyhyesti tiedoston lopun "Muutosloki"-osioon (uusin ylimpänä).

## MegaBonk-koonti

MegaBonk on 3D-muotoinen roguelike survival- eli bullet-heaven-peli. Pelaaja liikkuu itse, aseet hyökkäävät automaattisesti, vihollisia tulee jatkuvina aaltoina ja jokainen pelikerta rakentuu satunnaisista päivityksistä. Tavoite on selvitä mahdollisimman pitkään, voittaa alueiden bossit ja avata pysyvästi uusia hahmoja, aseita ja esineitä.

Peli sisältää 20 hahmoa, 30 asetta, yli 70 esinettä ja 240 tehtävää. Yksi pelikerta etenee näin:

1. Valitse hahmo ja aloita yleensä yhdellä aseella.
2. Liiku kartalla, tapa vihollisia ja kerää XP:tä, kultaa sekä esineitä.
3. Level up antaa satunnaisia ase-, Tome- tai stat-päivityksiä.
4. Tutki karttaa: arkut, shrinet, kauppias, salaisuudet ja bossiportaalit tarjoavat palkintoja tai riskejä.
5. Voita bossi ja jatka seuraavalle alueelle tai vaikeustasolle.
6. Kuoleman jälkeen käytä Silver-valuuttaa pysyviin avauksiin ja parannuksiin.

### Aseet

#### Aloitusaseet

- Sword: leveä lähitaistelusivallus. Hyötyy Damage-, Size-, Quantity- ja Cooldown-statseista.
- Flamewalker: jättää tulijäljen. Hyötyy Damage-, Duration-, Size- ja Cooldown-statseista.
- Lightning Staff: iskee lähellä oleviin vihollisiin ja voi ketjuttaa. Hyötyy Damage-, Quantity-, Bounces- ja Cooldown-statseista.
- Firestaff: räjähtäviä tulipalloja. Hyötyy Damage-, Projectile Count-, Size- ja Projectile Speed -statseista.
- Chunkers: hahmon ympärillä kiertäviä kiviä. Hyötyy Damage-, Quantity-, Size- ja Projectile Speed -statseista.
- Bone: vihollisista toiseen pomppiva luuammus. Hyötyy Damage-, Quantity-, Bounces- ja Projectile Speed -statseista.
- Bow: vihollisten läpi meneviä nuolia. Hyötyy Damage-, Quantity-, Crit- ja Projectile Speed -statseista.

#### Avattavat aseet

- Revolver: useita kimpoilevia luoteja. Hyötyy Damage-, Bounces-, Quantity- ja Cooldown-statseista.
- Aegis: torjuu vahinkoa ja vapauttaa vastaiskun.
- Bananarang: ulos lentävä ja takaisin palaava banaani.
- Aura: jatkuva vahinkokehä pelaajan ympärillä.
- Axe: pyörivät, laajasti osuvat kirveet. Hyötyy Damage-, Size-, Quantity- ja Cooldown-statseista.
- Space Noodle: linkittyy viholliseen ja vahingoittaa välissä olevia kohteita. Hyötyy Damage-, Duration-, Size- ja Cooldown-statseista.
- Sniper Rifle: hidas mutta erittäin voimakas läpäisevä laukaus.
- Slutty Rocket: hakeutuvat räjähtävät raketit. Hyötyy Damage-, Quantity-, Size- ja Cooldown-statseista.
- Shotgun: lyhyen kantaman haulikkoparvi. Hyötyy Damage-, Quantity-, Size- ja Cooldown-statseista.
- Mines: vihollisen kosketuksesta räjähtävät miinat. Hyötyy Damage-, Duration-, Size- ja Cooldown-statseista.
- Wireless Dagger: hakeutuvat tikarit, jotka voivat kimmota. Hyötyy Damage-, Bounces-, Quantity- ja Cooldown-statseista.
- Frostwalker: hidastaa ja jäädyttää vihollisia. Hyötyy Damage-, Duration-, Size- ja Cooldown-statseista.
- Tornado: työntää ja vahingoittaa vihollisia. Hyötyy Damage-, Duration-, Size- ja Cooldown-statseista.
- Dexecutioner: teräase, jolla on execute-mahdollisuus tavallisiin vihollisiin.
- Blood Magic: tapot voivat kasvattaa pysyvää Max HP:tä.
- Black Hole: vetää vihollisia kasaan ja vahingoittaa niitä. Hyötyy Damage-, Size-, Duration- ja Cooldown-statseista.
- Poison Flask: luo myrkyllisiä alueita. Hyötyy Damage-, Size-, Duration- ja Cooldown-statseista.
- Katana: nopeat automaattiset iskut lähimpään kohteeseen. Hyötyy Damage-, Cooldown-, Crit- ja Quantity-statseista.
- Dragon's Breath: automaattisesti suuntautuva tulikartio.
- Dice: satunnainen vahinko; hyvät heitot kasvattavat critiä.
- Hero Sword: voimakas lähitaistelu- ja etäviilto.
- Corrupted Sword: vahvistuu pelaajan HP:n ollessa matala.
- Scythe: 360 asteen viikatehyökkäys; elite-tapot lataavat vahvemman iskun.

### Aseiden tärkeimmät statsit

- Damage: kasvattaa aseiden vahinkoa.
- Cooldown / Attack Speed: lyhentää hyökkäysten välistä aikaa.
- Quantity / Projectile Count: kasvattaa ammusten tai hyökkäysten määrää.
- Size: kasvattaa ammuksen, auran, miekan tai räjähdyksen kokoa.
- Projectile Speed: kasvattaa ammusten nopeutta; joillakin aseilla se vaikuttaa myös kiertonopeuteen.
- Duration: kasvattaa tulijälkien, myrkkyalueiden, miinojen ja muiden efektien kestoa.
- Bounces: lisää pomppukertoja.
- Crit Chance: kriittisten osumien todennäköisyys.
- Crit Damage: kriittisten osumien lisävahinko.
- Knockback: työntää vihollisia kauemmas.
- Lifesteal: antaa mahdollisuuden parantaa yksi HP hyökkäyksellä.
- Armor: vähentää saatua vahinkoa.
- Evasion: antaa mahdollisuuden väistää isku kokonaan.
- Luck: parantaa harvinaisempien päivitysten, esineiden ja aseiden todennäköisyyttä.
- Movement Speed: nopeuttaa liikkumista ja voi olla hahmokohtaisesti myös vahinkostat.

Kaikki statsit eivät vaikuta kaikkiin aseisiin samalla tavalla. Esimerkiksi Quantity ei juuri hyödytä Auraa, kun taas Bone, Revolver, Bow ja Lightning Staff hyötyvät siitä paljon. Bounces on yleensä aseen oma päivitys, jota ei saa tavallisilla Tomeilla.

### Tomes, esineet ja shrinet

Tomeja ovat esimerkiksi Damage, Cooldown, Quantity, Size, Precision, Luck, Armor, Evasion, Movement Speed, Knockback, Cursed ja Bloody Tome. Ne vahvistavat tiettyjä statseja tai mahdollistavat erityisiä build-tyylejä.

Esineet antavat erikoisbonuksia ja muodostavat synergioita. Chonkplate kasvattaa kestävyyttä ja overhealia, Ice Cube tukee jäädytysbuildia, Anvil lisää aseiden päivityksiin ylimääräisen statin ja Microwave voi monistaa esineitä. Lisäksi pelissä on crit-, myrkky-, lifesteal- ja thorns-synergioita.

Shrinet ovat riski–palkinto-valintoja: ne voivat antaa tehokkaan palkinnon, mutta samalla viholliset, bossit tai runin vaikeus voivat vahvistua. Powerupit ovat väliaikaisia kentältä poimittavia bonuksia, eivät pysyviä esineitä.

### Esimerkkibuildeja

- Turvallinen aloittelijabuild: Flamewalker + Bow + Lightning Staff.
- Lähitaistelubuild: Katana + Aura + Axe tai Scythe.
- Crit-build: Dice + Revolver + Bow tai Sniper Rifle.
- Tank-build: Aegis + Aura + Armor, Thorns ja HP.
- Myrkkybuild: Poison Flask + Revolver + Duration ja Projectile Speed.
- Crowd-control-build: Black Hole + Frostwalker + Lightning Staff.

Tarkat perusvahingot, cooldownit ja upgrade-arvot riippuvat peliversiosta ja muuttuvat päivityksissä. Tämä koonti kuvaa aseiden toimintaa ja statseja yleisellä tasolla; numeeriset arvot kannattaa tarkistaa aina kyseisen version pelistä tai ajantasaisesta tietokannasta.

## Hahmot

Hahmot ovat selkeitä fantasy-/roguelite-arkkityyppejä. Jokaisella hahmolla on oma aloitusase, yksi vahva passiivi ja selkeä pelityyli. Visuaalinen tyyli on pelkistetty low-poly: suuret aseet, vahvat siluetit ja liioitellut animaatiot.

### Pottu

- Rooli: tasapainoinen lähitaistelija ja perushahmo.
- Aloitusase: Pannu.
- Pelityyli: luotettava lähietäisyyden vahinko, kohtuullinen HP, armor ja liike.
- Visuaalinen idea: ylisuuri metallikypärä, pienet saappaat ja raskas paistinpannu.

### Ritari

- Rooli: tankki ja lähitaistelija.
- Aloitusase: Sword tai Aegis.
- Pelityyli: paljon HP:tä ja armoria, pienempi liikkumisnopeus, vahva knockback ja turvallinen lähitaistelu.
- Passiivi-idea: osa osumista torjutaan kilvellä; torjunta voi ladata vastaiskun.

### Metsästäjä

- Rooli: etäisyysvahinko ja kriittiset osumat.
- Aloitusase: Bow.
- Pelityyli: suuri Projectile Speed, Crit Chance ja kantama, mutta heikompi HP ja Armor.
- Passiivi-idea: kaukana oleviin vihollisiin tehdään lisävahinkoa ja ensimmäinen osuma voi olla kriittinen.

### Velho

- Rooli: area damage ja elementtiefektit.
- Aloitusase: Lightning Staff tai Firestaff.
- Pelityyli: suuri Damage, Size, Quantity ja Duration; heikompi kestävyys ja liike.
- Passiivi-idea: aseiden elementtiefektit voivat levitä lähellä oleviin vihollisiin tai tehostua peräkkäisistä osumista.

### Ninja

- Rooli: nopea glass cannon ja väistöihin perustuva hahmo.
- Aloitusase: Wireless Dagger tai Katana.
- Pelityyli: erittäin suuri Movement Speed ja Evasion, nopea Attack Speed ja matala HP.
- Passiivi-idea: onnistunut väistö antaa hetkellisen hyökkäysnopeus- tai kriittisyysbonuksen.

### Berserkeri

- Rooli: aggressiivinen lähitaistelija, joka hyötyy matalasta HP:stä.
- Aloitusase: Axe tai Corrupted Sword.
- Pelityyli: erittäin suuri Damage ja Knockback, mutta puolustus heikkenee taistelun aikana.
- Passiivi-idea: vahinko kasvaa, kun HP laskee; elite-tapot voivat palauttaa osan HP:stä.

### Necromancer

- Rooli: kutsuihin ja tappojen ketjuttamiseen perustuva hahmo.
- Aloitusase: Bone tai Scythe.
- Pelityyli: heikompi suora vahinko, mutta tappojen jälkeen kentälle jääviä tai vihollisia jahtaavia luurankoja.
- Passiivi-idea: tietyllä tappomäärällä syntyy väliaikainen luurankosoturi tai luupallo.

### Paladiini

- Rooli: puolustava hybridihahmo.
- Aloitusase: Aura tai Aegis.
- Pelityyli: Armor, HP, Healing ja Knockback; hyökkäysnopeus on muita hahmoja pienempi.
- Passiivi-idea: pelaajan ympärillä oleva aura vahvistuu hetkeksi, kun pelaaja torjuu vahinkoa tai kerää healing-esineen.

### Alkemisti

- Rooli: myrkky-, tuli- ja räjähdysalueiden hallitsija.
- Aloitusase: Poison Flask tai Mines.
- Pelityyli: vahva Damage over Time, Duration ja Area; suora osumavahinko on heikompi.
- Passiivi-idea: samaan viholliseen kertyvät elementtiefektit voivat yhdistyä voimakkaaksi reaktioksi.

### Golem

- Rooli: hidas tankki ja alueen hallitsija.
- Aloitusase: Chunkers tai Aura.
- Pelityyli: erittäin suuri HP, Armor, Size ja Knockback, mutta pieni Movement Speed ja Attack Speed.
- Passiivi-idea: pelaaja ei voi joutua yhtä helposti työnnetyksi, ja vahingon saaminen lataa maahan iskeytyvän shokkiaallon.

### Pyromancer

- Rooli: tulivahinkoon ja suuriin alue-efekteihin erikoistunut taikuri.
- Aloitusase: Firestaff tai Flamewalker.
- Pelityyli: suuri Size, Duration ja Damage; tuliefektit sytyttävät vihollisia ketjureaktiona.
- Passiivi-idea: palavat viholliset jättävät kuollessaan lyhyen tulialueen.

### Onnenkorttihahmo

- Rooli: riskialtis, satunnaisuuteen ja harvinaisiin build-yhdistelmiin perustuva hahmo.
- Aloitusase: Dice.
- Pelityyli: korkea Luck ja vaihteleva vahinko; tasovalinnoissa ja arkuissa on parempi mahdollisuus harvinaisiin vaihtoehtoihin.
- Passiivi-idea: osa hyökkäyksistä voi olla poikkeuksellisen heikkoja tai erittäin voimakkaita; hyvät heitot voivat kasvattaa Crit Chancea hetkeksi.

Hahmojen lopulliset arvot kannattaa pitää maltillisina, jotta aseet, Tomes ja esineet ratkaisevat edelleen suurimman osan buildista. Hahmojen pitäisi tuntua erilaisilta jo aloitusaseen, yhden passiivin ja muutaman näkyvän perusstatin perusteella.

## Muutosloki

- 2026-09-09: **Tuhkaerämaa kytketty MapType-enumiin ja MAPIT-tabiin** (pelaaja-progressio-ja-ranked-suunnitelma.md, kohta 0.5/9) - Astran toimittamat `AshWastesEnvironment.cs`/`SurvivorAshWastesMap.cs` (Assets/Survivor/Scripts/) ja `AshWastesBuilder.cs` (Assets/Survivor/Editor/) kytketty pelikoodiin README-Tuhkaeramaa.md:n "Pelikoodin kytkentä" -ohjeen mukaisesti, samalla tavalla kuin Mosswood/Luuluola aikanaan.
  - `SurvivorProceduralMaps.cs`: uusi `MapType.AshWastes`-arvo enumiin. `BuildFixedMap(MapType)` saa kolmannen haaran, joka kutsuu `BuildAshWastesMap()`:ia (README:n ohjeistama funktio, jo olemassa `SurvivorAshWastesMap.cs`:ssä) saman vanhan-kartan-siivouksen jälkeen kuin Mosswood/Luuluola - `MapLayout` pysyy `null`:ina kuten muillakin kiinteillä kartoilla. `DrawMapTypeTab()`:in MAPIT-napit lisätty samaan `foreach`-taulukkoon Mosswood/BoneCaven rinnalle (3×Tier-nappia, sama `DrawMapTypeOption`-esitys ja -nimeäminen kuin muillakin, ei tarvinnut uutta UI-koodia).
  - `MapInventory.cs`: `MapTypeName` sai "Tuhkaerämaa"-nimen, `LoadMapInventory` lataa sen T1-omistuksen samalla mallilla. Kolmen kopion sijaan (`new[] { Mosswood, BoneCave }` esiintyi kolmessa paikassa) refaktoroitu yhdeksi `static readonly DroppableMapTypes`-taulukoksi, jota `TryDropMapItem` (nyt tasan 1/3 mahdollisuus per tyyppi `Random.Range`:llä 50/50:n sijaan), `MapInventorySummary` ja MAPIT-tabin nappilistaus kaikki lukevat - uuden kiinteän kartan lisäys vaatii jatkossa yhden rivin tähän taulukkoon, ei kolmea erillistä muokkausta.
  - **Tietoisesti ei koskettu**: `SurvivorGameNetwork.cs` (moninpelin karttasynkronointi ei tunne MapType/Tieriä millään kartalla - dokumentoitu tunnettu rajoitus, oma myöhempi tehtävänsä kohta 12), `AshWastesEnvironment.cs`/`SurvivorAshWastesMap.cs`/`AshWastesBuilder.cs` itse (Astran toimitus, jo tarkistettu Unityssä).
  - **Testattu Unity CLI:llä Play Modessa, täysi sykli**: Roslyn-evalilla myönnetty AshWastes T1 -karttaesine, valittu MAPIT-tabin tilaa vastaavasti (`selectedMapType`/`selectedMapTier`), `StartGame()` → `SkipLoadout()` (Mappi-tilan loadout-valinta, samalla polulla kuin oikea pelaaja) → tarkistettu `activeMapType=AshWastes`, `fixedMapRoot`=`Tuhkaeramaa_Environment(Clone)` (40 lasta), `forestObstacles.Count=16` (täsmää README:n "16 maastoestettä"), `MapLayout=null` (oikein). `SelectStarter(Sword)` ja 5 sekuntia oikeaa Play Mode -aikaa taistelua - 7 vihollista hengissä, `activeMapType` pysyi AshWastes:ssä koko ajan. `ExitToMainMenu()` palautti `AtMainMenu=true` siististi. **Kontrollitesti**: sama 5 sekunnin taistelusykli Proceduralilla (Selviytymistilan kartta) tuotti täsmälleen saman `NetworkObject.OnDestroy`-`NullReferenceException`-virhesarjan kuin AshWastes-testi (vahvistettu: kaikilla 7 vihollisella on `NetworkObject`-komponentti, virhe tulee Netcode-paketista kun se tuhoutuu ilman aktiivista verkkosessiota Editorissa) - **tämä on siis olemassa oleva, karttatyypistä riippumaton virhe, ei AshWastes-regressio**, ei kirjattu tähän tehtävään korjattavaksi. Karttaesinemäärä ja PlayerPrefs palautettu testiä edeltävään tilaan (0 kpl) puhdistuksessa. Käännös 0 virhettä. Ei koskettu Assets/WeaponArtPilot/-, WeaponBases/- tai WeaponAttachmentPoint.cs/SwordVariantVisual.cs/WeaponAffixes.cs-tiedostoihin - `git status` näytti edelleen kolmannen osapuolen keskeneräistä työtä samoissa jaetuissa tiedostoissa (SurvivorArsenal.cs/SurvivorGame.cs/SurvivorProgression.cs ym.) johon ei koskettu; commit eristettiin samalla HEAD+omat-muutokset-menettelyllä kuin affiksierissä.

- 2026-09-09: **Ase-affiksijärjestelmän laajennus 6 lisäaseelle** (pelaaja-progressio-ja-ranked-suunnitelma.md, kohta 10.3) - Mines, Tornado, Poison Flask, Space Noodle, Slutty Rocket, Wireless Dagger. Affiksit ovat nyt 19/30 aseella (13 aiempaa + tämä 6). Ei koskettu Assets/WeaponArtPilot/-, WeaponBases/- tai WeaponAttachmentPoint.cs/SwordVariantVisual.cs-tiedostoihin (rajaus, Astra saattoi työskennellä samaan aikaan asset-puolella) - `git status` näytti myös kolmannen osapuolen keskeneräistä työtä (SurvivorArsenal.cs/SurvivorGame.cs/SurvivorProgression.cs, uudet Art_Prototype_Stylized/Feyloom/WeaponVfxBuilder.cs/AshWastesBuilder.cs) johon ei koskettu millään tavalla; tämä commit eristettiin täsmällisesti niistä samalla HEAD+omat-muutokset-menettelyllä kuin kahdessa aiemmassa affiksierässä (ks. alla).
  - **AGENTS.md**: kirjattu puuttuneet statshyötylistat kuudelle aseelle ("Hyötyy X-, Y-, Z- ja W-statseista") samalla tyylillä kuin aiemmilla 13:lla - Mines (Damage/Duration/Size/Cooldown), Tornado (Damage/Duration/Size/Cooldown), Poison Flask (Damage/Size/Duration/Cooldown), Space Noodle (Damage/Duration/Size/Cooldown), Slutty Rocket (Damage/Quantity/Size/Cooldown), Wireless Dagger (Damage/Bounces/Quantity/Cooldown).
  - **`WeaponAffixes.cs` refaktoroitu kohdan 10.5 kuvaaman mallin mukaiseksi**: `AffixPool` ei ole enää käsin kirjoitettu taulukko, vaan lazily rakennettu `WeaponStats.cs`:n `WeaponMetadata[w].applicableStats`:sta uudelle `AffixEnabledWeapons`-listalle (19 asetta) - yhden totuuden lähde koodissa ja AGENTS.md:ssä molemmilla puolilla, uuden aseen käyttöönotto vaatii jatkossa enää yhden rivin lisäyksen tähän listaan (kunhan applicableStats+AGENTS.md-lista on jo olemassa). Lazy-rakennus (ei staattinen kenttäalustus) koska `WeaponMetadata` asuu eri partial-class-tiedostossa (WeaponStats.cs) - staattisten kenttien alustusjärjestykseen eri tiedostojen välillä ei voi luottaa.
  - **Valinta perusteltu koodista käsin**: 6 asetta valittiin siitä syystä että niiden `CastAdvanced`/`AddAdvancedShot`/`AddAdvancedZone`-koodi käytti jo `stats.Size`/`stats.Duration`/`stats.Quantity`/`stats.Bounces`-kertoimia identtisellä kaavalla kuin `BlackHole` (jolla affiksit toimivat jo ennestään) - varmistettiin lukemalla koodi ensin. Yksi mahdollinen 7. ehdokas (Sniper) jätettiin tarkoituksella pois, koska sen `LineHit`-kutsu ei lukenut `stats.Size`:a ollenkaan - Size-affiksi ei olisi vaikuttanut mihinkään, ja se korjaus kuuluu vasta Sniperin omaan käyttöönottotehtävään. Rajaukset ennallaan: max 2 affiksislottia, +15-35 %, vain omistetuille aseille, vain Mappi-tilassa (`AffixMultiplier`/`AffixMultiplierFor` samat vartijat kuin ennen), Selviytymistila koskematon (`GetWeaponStats` palauttaa `Identity`:n Proceduralissa, testattu erikseen ettei `activeMapType` jäänyt vahingossa Mosswoodiin - palautettiin Proceduraliksi joka testilohkon jälkeen).
  - **Testattu Unity CLI:llä Play Modessa, samalla suoran laskennan menetelmällä kuin aiemmat 13 asetta**: Roslyn-evalilla pakotettu tunnettu +20 % Damage-affiksi suoraan `weaponAffixes`-dictionaryyn (reflektiolla, ohittaen `RollAffix`:in satunnaisuuden) kullekin 6 aseelle, laukaistu oikea `CastAdvanced(w,level)`-funktio dummy-vihollista vasten (koska Tornado/PoisonFlask/SpaceNoodle/Rocket/WirelessDagger vaativat `Nearest()`:in löytävän kohteen ennen kuin ne tuottavat mitään) ja mitattu syntyneen `AdvancedShot`/`AdvancedZone`-olion `power`-kenttä ennen/jälkeen - **kaikki 6 asetta antoivat tasan 1,2 (base=18→buffed=21,6 tai base=36→43,2 Rocketilla)**, ei yhtään regressiota. Pistokoe muille statseille samalla menetelmällä: Mines Size (2,8→3,36, ratio 1,2), Mines Duration (10→12, ratio 1,2), WirelessDagger Bounces tasolla 3 (baseBounces=2→3, +50 % affiksi), Rocket Quantity (+250 % pakotettu ääriarvo baseQty=1:stä, 1→3 ammusta, `Mathf.RoundToInt`-kaavan mukaisesti) - kaikki täsmäsivät odotettuun kaavaan. **Droppijakauma-otos**: `RollAffix(Mines,...)` 4000 kertaa - vain neljä oikeaa statia (Damage/Duration/Size/Cooldown) esiintyi, ~25 % jokainen (24,7-25,3 %), kaikki prosentit välillä 15-35 %. `TryDropWeaponAffixMaterial()` 3000 kertaa Mines omistettuna - 11,33 % osumaprosentti (odotus 12 %, ero selittyy näyteotoksen vaihtelulla, ~1,1 keskihajonnan sisällä). 0 konsolivirhettä testien aikana. Käännös 0 virhettä.

- 2026-09-09: **Yhtenäinen WeaponStats-pipeline** (pelaaja-progressio-ja-ranked-suunnitelma.md, kohta 10.5) - refaktoroi nykyisten 13 affiksi-/varianttiaseen bespoke-taistelukoodin yhdeksi keskitetyksi putkeksi, ennen affiksien laajennusta lopuille 17 aseelle. Puhdas sisäinen refaktorointi - pelaajalle näkyvä käytös ei muuttunut, vain koodin rakenne. Toteutettu vaiheittain, käyttäjä vahvisti kunkin vaiheen ennen seuraavaa:
  - **Vaihe 1 (kartoitus)**: listattu kaikkien 30 aseen statshyötylistat AGENTS.md:stä ja kunkin toteutetun aseen ajastinmalli (jaettu `attackTimer`: Sword/Bow/Lightning; `weaponTimers[6]`: Flamewalker/Firestaff/Chunkers/Bone/Aura/Shotgun; `advancedTimers[21]`: loput 24). Vahvistettu 8 statin lopullinen lista (Damage/Cooldown/Size/Quantity/Duration/ProjectileSpeed/Bounces/Crit, sama kuin nykyinen `AffixStat`-enum) - Pierce/Bounces pidetään tarkoituksella yhtenä statina toiseksi eriyttämiseen ei ole konkreettista tarvetta.
  - **Vaihe 2 (data)**: uusi `WeaponStats.cs` - `WeaponArchetype`-enum (Melee/RangedProjectile/Orbit/AoE/Beam) ja `WeaponMeta{archetype, applicableStats}` per kaikki 30 asetta (13 toteutetun aseen `applicableStats` identtinen nykyisen `AffixPool`:in kanssa, loput 17 päätelty `CastAdvanced`/`CastArsenal`-mekaniikasta, ei vielä käytössä). Ei kytkentää taisteluun tässä vaiheessa.
  - **Vaihe 3 (`GetWeaponStats`)**: `GetWeaponStats(Weapon)` (Selviytymistila-portitettu, kuten `AffixMultiplier`) ja `GetWeaponStatsFor(character,weapon)` (ungated, UI/testaus) palauttavat `WeaponStats`-structin, jossa kaikki 8 kenttää ovat kertoimia (1=ei affiksia) - täsmälleen samat arvot kuin hajanaiset `AffixMultiplier(w,stat)`-kutsut palauttaisivat, ei uusia kertoimia. **Tietoinen rajaus, kirjattu erilliseksi tehtäväksi**: `WeaponStats.Crit` pysyy paikallisena vahinkokertoimena (Bow/Katana-malli), ei muutettu globaaliksi CritChance/CritMultiplier-lisäykseksi - se olisi aidosti pelaajalle näkyvä käytösmuutos (crit-tapahtumat/-visuaali muuttuisivat), joten "Crit yleiseksi" jätetty omaksi, myöhemmin erikseen hyväksyttäväksi ja testattavaksi tehtäväksi. `VariantQualityMultiplier` pysyy myös erillisenä (sovelletaan jo ehdollisesti vain varianttihaaroissa, esim. Sword vain twinblade-iskussa) - sen leipominen `WeaponStats`:iin olisi muuttanut käytöstä ei-variantti-haaroissa.
  - **Vaihe 4 (ajastintaulukko)**: `Combatant.AttackTimerSlots[30]` (yksi slotti per `Weapon`-enumin arvo) ja `WeaponInterval(Weapon)` (perusintervallit koottu yhteen kolmen hajanaisen lähteen - Sword/Bow/Lightning=1, `TickArsenal`-ternääri, `AdvancedCatalog.Entries` - sijaan). **Tietoinen rajaus, kirjattu erilliseksi tehtäväksi**: Sword/Bow/Lightning jakavat yhä tarkoituksella yhden slotin (Sword-indeksi) `SharedAttackTimerAffixMultiplier`:in kautta - täsmällisten erillisten per-ase-slottien antaminen niille korjaisi dokumentoidun jaetun-ajastimen kompromissin, mutta muuttaisi käytöstä siinä reunatapauksessa että molemmilla on samaan aikaan eri Cooldown-affiksi. Jätetty bittitäsmällisesti ennalleen, korjaus omaksi tehtäväksi "Crit yleiseksi":n rinnalle.
  - **Vaihe 5 (migraatio)**: kaikki 13 asetta siirretty käyttämään `GetWeaponStats()`+yhtenäistä ajastintaulukkoa - `AttackWeapons()` (Sword/Bow/Lightning, `SurvivorProgression.cs`), `CastArsenal()`/`TickArsenal()`/`TickOrbits()` (Flamewalker/Firestaff/Chunkers/Bone/Shotgun, `SurvivorArsenal.cs`), `CastAdvanced()`/`TickAdvanced()` (Revolver/Axe/Katana/Frostwalker/BlackHole, `SurvivorAdvancedWeapons.cs`). Vanhat `weaponTimers`/`advancedTimers`-taulukot ja niiden property-wrapperit poistettu kokonaan käytöstä. Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa: **per-ase regressiotestaus kaikille 13 aseelle** samalla suoran laskennan menetelmällä kuin affiksit on jo testattu - pakotettu tunnettu +20 % Damage-affiksi per ase, laukaistu oikea hyökkäysfunktio (`AttackWeapons`/`CastArsenal`/`CastAdvanced`/`TickOrbits`), mitattu ennen/jälkeen-vahinkosuhde (Hit()-delta tai luodun shot/flame/zone-olion `power`-kenttä) - **kaikki 13 asetta antoivat tasan 1,2 (±0,0002 float-pyöristys)**, ei yhtään regressiota. Ohessa löydetty ja korjattu testimenetelmä-artefakti: ensimmäinen Sword-testi käytti 1 000 000 HP:n vertailuarvoa, jonka float32-tarkkuus lähellä sitä magnitudia (~0,0625 ULP) vääristi 11,7:n kaltaiset pienet deltat (11,6875) - vaihdettu 10 000 HP:n perustaso poisti artefaktin täysin, varmennettu suoralla `Hit(enemy,11.7f)`-kutsulla molemmilla perustasoilla. **Ajastinjärjestelmän regressiotestaus**: Cooldown-affiksi testattu Shotgunilla (weaponTimers-alkuperä) ja Axella (advancedTimers-alkuperä) - molemmat antoivat tasan 1,25-suhteen; jaetun Sword+Lightning-slotin yhdistetty kerroin testattu `SharedAttackTimerAffixMultiplier`:illa - täsmälleen 1 / 1,25 / 1,375 (bittitäsmällisesti identtinen ennen-migraatiota-kaavaan). **Selviytymistila-0-vaikutus** varmennettu Axella (`CastAdvanced`) - Mappi-tilassa +20 % Damage-affiksilla `AdvancedShot.power`=32,4, Selviytymistilassa tasan 27 (ei affiksia). Ei yhtään konsolivirhettä koko testiajon aikana. Testidata siivottu `PlayerPrefs`ista jälkeenpäin. **Git-eristys**: `git status` paljasti jälleen kolmannen kerran toisen, tähän liittymättömän istunnon keskeneräisen työn samoissa jaetuissa tiedostoissa (ase-VFX-poolisysteemi `WeaponVfxPool.cs`/`WeaponVfxCatalog.cs`, Necromancer-boss). Kaikki neljä jaettua tiedostoa (`SurvivorGame.cs`/`SurvivorProgression.cs`/`SurvivorArsenal.cs`/`SurvivorAdvancedWeapons.cs`) sisälsivät tällä kertaa omia muutoksia, joten kukin eroteltiin samalla HEAD+omat-muutokset-menettelyllä kuin aiemmin (varmennettu `diff`:illä molempiin suuntiin per tiedosto). `SurvivorAdvancedWeapons.cs`:n eristyksessä väliaikainen committiin menevä versio olisi törmännyt kääntäjässä `WeaponVfxPool.cs`:n kanssa (molemmat määrittelevät `SpawnImpact`-metodin) - tämä ei haitannut, koska väliaikainen tiedosto oli vain `git add`/`git commit`-hetken ajan levyllä, ei koskaan käännetty sellaisenaan; käännös varmistettu vasta lopullisesta, molemmat istunnot yhdistävästä palautetusta tilasta. Committattu.

- 2026-09-09: **Ase-affiksijärjestelmä laajennettu kuudelle avattavalle aseelle** (`44ee574`/`1fc7f1b`-pilotin jatko): Revolver (Damage/Bounces/Quantity/Cooldown), Axe (Damage/Size/Quantity/Cooldown), Katana (Damage/Cooldown/Crit/Quantity), Shotgun (Damage/Quantity/Size/Cooldown), Frostwalker (Damage/Duration/Size/Cooldown), Black Hole (Damage/Size/Duration/Cooldown) - statshyötylistat lisätty AGENTS.md:n "Avattavat aseet" -osioon samaan tyyliin kuin aloitusaseilla, `AffixPool`-merkinnät `WeaponAffixes.cs`:ään. Shotgun kulkee jo pilotissa yleistetyn `CastArsenal()`/`TickArsenal()`-koodin läpi (`SurvivorArsenal.cs`), joten se ei tarvinnut yhtään uutta taistelukoodia - pelkkä `AffixPool`-rivi riitti. Loput viisi kytketty `CastAdvanced()`/`TickAdvanced()`-reittiin (`SurvivorAdvancedWeapons.cs`) samalla per-ase-kaava-periaatteella kuin pilotin seitsemällä aseella: jokaisella on oma `advancedTimers[i]`-ajastin (kuten Flamewalkerilla pilotissa), joten Cooldown-affiksi ei vaadi Sword/Lightning-tyyppistä jaetun-ajastimen kompromissia yhdellekään näistä kuudesta. Katanan Crit-affiksi taipuu paikalliseksi vahinkokertoimeksi (sama ratkaisu kuin Bow'lla pilotissa, koska globaalia per-ase crit-hookia ei ole). Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa reflection-pohjaisilla `eval`-kutsuilla samalla menetelmällä kuin pilotissa: **droppijakauma** laajennetulla 13 aseen poolilla 4000 kutsun otoksella kummallekin materiaalille (Lisää-materiaali 11,25 %, Rulla-materiaali 11,125 %, tavoite 12 % - Axe omistettuna vaikutti droppiin oikein). **Affiksislottien täyttyminen** Axella tarkistettu samalla tavalla kuin pilotissa (ilman materiaalia epäonnistuu, kaksi ensimmäistä onnistuu, kolmas hylätään slottien täyttyessä). **Taistelukytkentä suoralla laskennalla (Axe)**: pakotettiin tunnettu +25 % Damage-affiksi, laukaistiin oikea `CastAdvanced(Axe,1)` kenttää-olevaa vihollista vasten - syntyneen `AdvancedShot.power`=33,75 täsmäsi tarkalleen kaavaan 18×1,25×1,5 (Axen oma 1,5x-kerroin `AddAdvancedShot`:issa); Selviytymistilassa sama kutsu antoi tasan 27 (=18×1,5, ei affiksia) - osoittaa affiksin vaikuttavan täsmälleen oikealla kertoimella Mappi-tilassa ja katoavan täysin Selviytymistilassa. Ei yhtään konsolivirhettä koko testiajon aikana (parempi tulos kuin pilotissa, jossa esiintyi tunnettu `choices[i]`-testiartefakti). Testidata siivottu `PlayerPrefs`ista jälkeenpäin. **Git-eristys**: `git status` paljasti jälleen toisen, tähän liittymättömän istunnon keskeneräisen työn samoissa jaetuissa tiedostoissa (`SurvivorAdvancedWeapons.cs`:n ase-VFX-poolikutsut `AttachWeaponVfx`/`WeaponZoneMaterial`/`ImpactEffectFor`, sekä `SurvivorGame.cs`/`SurvivorProgression.cs`/`SurvivorArsenal.cs`:n VFX/Necromancer-bossimuutokset, jotka olivat jälleen ilmestyneet työhakemistoon istuntojen välissä) - näistä vain `SurvivorAdvancedWeapons.cs` sisälsi tämän kerran omia affiksimuutoksia, joten se eroteltiin samalla HEAD+omat-muutokset-menettelyllä kuin pilotissa (varmennettu `diff`:illä molempiin suuntiin: committiin menevä versio täsmäsi tarkalleen HEAD:iin sovellettuihin affiksimuutoksiin, ja committin jälkeen palautettu työkopio täsmäsi tarkalleen alkuperäiseen `diff -q`:lla), kun taas `SurvivorGame.cs`/`SurvivorProgression.cs`/`SurvivorArsenal.cs` eivät sisältäneet tämän istunnon muutoksia lainkaan ja jätettiin kokonaan committin ulkopuolelle. Committattu (`1669638`).

- 2026-09-09: **Ase-affiksien craftausjärjestelmä.** Uusi tiedosto `WeaponAffixes.cs` (`WeaponVariants.cs`:n malliin). Laajuus rajattu suunnitteluvaiheessa hyväksytysti seitsemään aloitusaseeseen (Sword, Flamewalker, Lightning, Firestaff, Chunkers, Bone, Bow) - AGENTS.md:n ainoat aseet joilla on dokumentoitu per-ase statshyötylista (rivit 75-81); loput 23 asetta eivät ole mukana, laajennettavissa myöhemmin samaan tapaan kuin variantit.
  - **Data**: `Dictionary<(PlayerCharacter,Weapon), List<WeaponAffix>>` (max 2 slottia/ase, `struct WeaponAffix { AffixStat stat; float percent; }`, +15-35% rullattu), tallennus `PlayerPrefs`iin per hahmo samaan tapaan kuin `SaveActiveVariants`. Affiksipooli per ase (`AffixPool`) suoraan AGENTS.md:n statslistoista, esim. Sword: Damage/Size/Quantity/Cooldown.
  - **Taistelukytkentä**: ei yhtä keskitettyä kerrointa, koska pelissä ei ole yleistä stat-putkea - jokainen ase laskee vahinkonsa/kokonsa/määränsä erikseen. `AffixMultiplier(Weapon,AffixStat)` palauttaa 1+affiksin kertoimen (tai tarkan 1 Selviytymistilassa, ks. alla) ja on ketjutettu jokaisen 7 aseen omaan kaavaan `AttackWeapons()`:issa (Sword/Bow/Lightning, `SurvivorProgression.cs`) ja `CastArsenal()`/`TickOrbits()`:issa (Flamewalker/Firestaff/Bone/Chunkers, `SurvivorArsenal.cs`), samaan tapaan kuin `VariantQualityMultiplier` on jo ketjutettu. Kaksi arkkitehtuurirajoitusta jouduttiin ratkaisemaan pragmaattisesti: (1) Sword ja Lightning jakavat `SurvivorGame`:n yhden `attackTimer`:in (ei per-ase ajastinta), joten niiden Cooldown-affiksi nopeuttaa koko jaettua sykettä sen ajan kun ase on omistettu - sama tapa kuin Cooldown Tome jo toimii, ei siis regressio. Flamewalker/Firestaff/Bone/Chunkers-räjähdysvariantilla ON omat `weaponTimers[i]`-ajastimet, joten niiden Cooldown-affiksi toimii täsmällisen per-ase-tarkasti. (2) Bow:n Crit-affiksi ei koske globaalia `CritChance`:a (joka vaikuttaisi kaikkiin aseisiin) vaan on toteutettu ylimääräisenä vahinkokertoimena `Bolt.power`:iin - sama "paikallinen kaava" -periaate kuin muillakin affikseilla.
  - **Rajaukset**: `AffixMultiplier` palauttaa aina tasan 1 kun `activeMapType==MapType.Procedural`, joten tallennetut affiksit eivät vaikuta mitenkään Selviytymistilan taisteluun vaikka niitä olisi rullattu. Kaksi uutta materiaalia (`affixAddMaterial`, `affixRerollMaterial`) droppaavat vain `TryDropWeaponAffixMaterial()`/`TryDropAffixRerollMaterial()`:illä (`Hit()`:in bossinkaatohaara, `SurvivorGame.cs`) - oma riippumaton 12 % rulla kummallekin, vain Mappi-tilassa, vain omistetulle pilottiaseelle. `TryAddAffix`/`TryRerollAffixes` (kulutus) EI tarkista `activeMapType`:a, samaan tapaan kuin `UpgradeVariantQuality` - ne käytetään HAHMOT-tabin kaupassa (päävalikko) eikä kesken runin, joten materiaalien droppaus- ja kulutusrajaus ovat tarkoituksella eri paikoissa.
  - **UI**: HAHMOT-tabin ase-ruudukko (`DrawDefaultLoadoutSection`, `CharacterShop.cs`) - variantti-/laatu-rivien alle uusi affiksirivi (teksti, esim. "Affiksit: Dmg +22% Size +18%") ja kaksi puolikkaan levyistä nappia "Lisää (N)"/"Rulla (N)", harmaina kun materiaalia ei ole tai slotit täynnä. `rowPitch` kasvatettu kahdella uudella rivikorkeudella kaikille korteille tasaisesti (sama malli kuin variantti-/laatu-rivit, jotka jo varaavat tilan tyhjänä ei-varianttiaseille).
  - Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa reflection-pohjaisilla `eval`-kutsuilla (koska HAHMOT-tab on OnGUI eikä täysin klikattavissa tässä istunnossa): **droppijakauma** 4000 kutsun otoksella kummallekin materiaalille erikseen (Lisää-materiaali 11,3 %, Rulla-materiaali 11,7 %, tavoite 12 % - molemmat riittävän lähellä ja toisistaan riippumattomia); **Selviytymistilassa droppi täsmälleen 0/3000+3000** kummallekin. **`TryAddAffix`**: ilman materiaalia epäonnistuu eikä lisää mitään; materiaalilla kaksi ensimmäistä onnistuu (2/2 slottia täynnä); kolmas epäonnistuu slottien täyttyessä eikä kuluta materiaalia (5→3, ei 5→2). **`TryRerollAffixes`**: ilman materiaalia epäonnistuu, vanha lista koskematon; materiaalilla tyhjentää ja täyttää tasan 2 uutta arvoa väliltä 15-35 %, kuluttaa tasan yhden materiaalin. **Taistelukytkentä suoralla laskennalla (Sword)**: pakotettiin tunnettu +20 % Damage-affiksi `weaponAffixes`-dictiin reflectionilla, varmistettiin `AffixMultiplierFor`=1,2 ja yksityinen taistelukoodin käyttämä `AffixMultiplier`=1,2 Mappi-tilassa/tasan 1 Selviytymistilassa; laukaistiin oikea `AttackWeapons()` kentällä olevaa vihollista vasten (`fullPower`=18×1,2=21,6 odotettu) - mitattu vahinko 14,04 täsmäsi tarkalleen kun huomioitiin tallenteessa jo aiemmin päällä ollut Kaksoisterä-variantti (21,6×0,65-kerroin=14,04); Selviytymistilassa sama isku samalle viholliselle antoi tasan 11,70 (=18×0,65, ei affiksia) - osoittaa affiksin vaikuttavan täsmälleen oikealla kertoimella Mappi-tilassa ja katoavan täysin Selviytymistilassa. Testien jälkeen `PlayerPrefs`-testidata (Pottu/Sword-affiksit + molemmat materiaalilaskurit) poistettu ja tila ladattu uudelleen puhtaana. Konsolissa esiintyi testien aikana toistuva `ArgumentOutOfRangeException` (`SurvivorProgression.cs:445`, `choices[i]`) - jäljitetty täysin erilliseksi, jo olemassa olevaksi UI-koodiksi joka liittyy `PendingChoices`/`choices`-tilaan; syntyi koska testit ohittivat normaalin `ResetRun()`→`BeginMapRun()`-kulun reflectionilla eivätkä koskaan täyttäneet `choices`-listaa oikein - ei yhtään virhettä millään uuden affiksikoodin rivillä (tarkistettu hakemalla koko lokista `WeaponAffixes`/`CharacterShop.cs`/`SurvivorArsenal.cs`/`AffixMultiplier`/`TryAddAffix`/`TryRerollAffixes` - 0 osumaa). **Committattu (`44ee574`)**: `AGENTS.md`/`CharacterShop.cs`/`SurvivorGame.cs`/`SurvivorArsenal.cs`/`SurvivorProgression.cs`/uusi `WeaponAffixes.cs`+`.meta`. `git status` paljasti samaan aikaan työhakemistossa toisen, tähän liittymättömän keskeneräisen istunnon työn (ase-VFX-poolijärjestelmä `WeaponVfxPool.cs`/`WeaponVfxCatalog.cs` + `AttachWeaponVfx`/`WeaponZoneMaterial`-kutsut samoissa tiedostoissa, Necromancer-bossin `CreateNecromancerBoss`/`boss.animator`, Tuhkaerämaa-taidepaketti, `Packages/manifest.json`) - näiden viiden jaetun tiedoston (`SurvivorGame.cs`/`SurvivorArsenal.cs`/`SurvivorProgression.cs` + kaksi ei-affiksiin liittyvää) kohdalla affiksimuutokset eroteltiin täsmällisesti: jokaisen tiedoston HEAD-versioon sovellettiin vain tämän istunnon tarkat affiksimuutokset (varmennettu `diff`:illä täsmäämään täysin HEAD+affiksit-yhdistelmää vastaan ennen commitointia), committiin, minkä jälkeen toisen istunnon WIP-rivit palautettiin tarkalleen takaisin työhakemistoon (varmennettu `diff -q` alkuperäistä työkopiota vastaan - täsmäsi paitsi yksi harmiton rivinvaihtoero). Käännös tarkistettu committin jälkeen palautetulla, molempien istuntojen yhdistelmällä: 0 virhettä.

- 2026-09-09: Tuhkaerämaa-ympäristön muokattava taidepaketti: `AshWastesEnvironment.cs` (koodimallinnus), `AshWastesBuilder.cs` (Editor-rakennus), `Resources/AshWastes/Tuhkaeramaa_Environment.prefab`, jaetut mesh-assetit ja seitsemän URP-materiaalia. Oranssi/ruskea/tummanpunainen paletti, hiiltyneet puut, halkaistut monoliitit, basalttikehät ja emissiiviset halkeamat; viisi maamerkkiryhmää nykyisten arkkujen/pyhäkön/portaalin kohdilla, avoin tasainen maa. `BuildAshWastesMap()` valmis kiinteän kartan kytkentää varten, mutta MapType-/inventory-/drop-listoja ei tässä taidetoimituksessa muutettu. Unityssä rakennettu ja visuaalisesti tarkistettu; kuusi vapaata sijoitusankkuria, 16 estettä ja asset-viittaukset tarkistettu. Ohje `README-Tuhkaeramaa.md`. Ei tekstuuririippuvuuksia, ei uutta sceneä tai julkaisua.

- 2026-09-09: **UX-korjaus: PELAA-nappi MAPIT-tabille.** Käyttäjä testasi ensimmäistä kertaa oikeaa buildia (ei `eval`-kutsuja) ja huomasi, että MAPIT-tabilla valittu kartta (esim. Luuluola T1, näkyi korostettuna) ei ollut käynnistettävissä siitä ruudusta - PELAA oli vain KOTI-tabilla. Vahvistettu ensin `eval`-kutsuilla että `selectedMapType`/`selectedMapTier` ovat tavallisia kenttiä joita mikään tabinvaihto ei koske - ne säilyivät muuttumattomina MAPIT→KOTI-vaihdossa, joten kyse oli pelkästä UX-selkeydestä, ei toiminnallisesta bugista. Lisätty `DrawMapTypeTab()`:iin (`SurvivorProceduralMaps.cs`) oma PELAA-nappi rivien alle, joka kertoo suoraan mikä on valittuna ("PELAA VALITULLA KARTALLA [LUULUOLA T1]" / "[SATUNNAINEN]") ja kutsuu samaa `StartGame()`:ia kuin KOTI-tabin nappi. **Tässä istunnossa ei ole hiiriklikkaustyökalua Unity Game View'lle** (vain Chrome-selaimen `computer`-työkalulle sellainen on) - joten "oikean buildin" testaus tehtiin kutsumalla täsmälleen niitä samoja kenttäasetuksia/metodeja joita napit itse kutsuvat (`selectedMapType`/`selectedMapTier`-kenttien asetus = mitä MAPIT-napin klikkaus tekee, `StartGame()`-kutsu = mitä PELAA-napin klikkaus tekee), ei ohitettu mitään UI-logiikkaa. Play Modessa varmennettu: MAPIT-tabilla Luuluola T1 valittiin ja korostui kuvakaappauksella uuden PELAA-napin kanssa; vaihdettiin KOTI-tabille ja varmistettiin `selectedMapType`/`Tier` säilyivät (BoneCave/1); palattiin MAPIT-tabille ja "painettiin" uutta PELAA-nappia (`StartGame()`) - `activeMapType`=BoneCave, `activeMapTier`=1, `MapLayout`=null (kiinteä kartta) hahmovalinnan/loadoutin jälkeen, kuvakaappaus vahvisti Luuluolan latautuneen oikein pelattavaksi. Ei uusia konsolivirheitä paitsi tuttu Netcode-dispose Play Moden pysäytyksestä (jo aiemmin dokumentoitu, ei liity tähän). Committattu.

- 2026-09-09: Committattu Mappi-inventaario nykyisessä muodossaan (ks. alempi 2026-09-09-merkintä) - koska `SurvivorProceduralMaps.cs` ja `SurvivorProgression.cs` sisälsivät samaan aikaan myös toisen, tähän liittymättömän keskeneräisen työn rivejä (`BuildInfernoBossArena()`-kutsu, drop-imun `TickMapTransition`-laajennus, `CreateNecromancerBoss`/`boss.animator`/`AttachWeaponVfx`/`SpawnImpact`), nuo neljä kohtaa palautettiin väliaikaisesti HEAD:in muotoon ennen commitointia ja palautettiin takaisin nykyiseen muotoonsa heti commitin jälkeen (diff täsmäsi tarkalleen alkuperäiseen) - committiin päätyi vain MAPIT-tabin kytkentä + Mappi-inventaario, ei muiden istuntojen keskeneräistä työtä. Sen jälkeen laajennettu systeemi Tierillä (1-3): `TryDropMapItem()` (`MapInventory.cs`) rullaa nyt myös Tierin painotetusti `RollMapTier()`:illä (60/30/10 %) kiinteän 1:n sijaan, ja ilmoitus näyttää Tierin ("Löysit Karttaesineen: Luuluola T2!"). Uudet kentät `selectedMapTier`/`activeMapTier` (`SurvivorProceduralMaps.cs`, sama lukitusmalli kuin `activeMapType`:lla) ja `MapTierMultiplier` (`1 + (activeMapTier-1)*.35`, eli T1=1.0x/T2=1.35x/T3=1.7x) - Procedural pysyy aina Tier 1:ssä eikä `BeginMapRun()` koskaan aseta sille muuta, joten proseduraalinen kartta ei muutu miltään osin. `BeginMapRun()` kuluttaa nyt tarkan (tyyppi,Tier)-parin `ConsumeMapItem(activeMapType, activeMapTier)`:illä; jos juuri se Tier on loppu, pudotaan turvallisesti Procedural/T1:een kuten aiemminkin. `AdvanceArea()` ei tarvinnut muutosta - se käyttää jo lukittua `activeMapType`:a fixed-map-haarassaan, ja `MapTierMultiplier` lukee `activeMapTier`:ia joka pysyy samana koko pelikerran ajan. Vihollisten skaalaus (`SurvivorGame.cs`): `enemyCap` ja `SpawnEnemy()`:in `health`-laskukaava kerrotaan `MapTierMultiplier`:lla olemassa olevan Elapsed/curse/Area-skaalauksen päälle (nopeutta ei kosketa, tehtävänannon mukaisesti vain "HP:n/katon"). MAPIT-tab (`DrawMapTypeTab()`/`DrawMapTypeOption()`) listaa nyt seitsemän riviä: SATUNNAINEN (Tier 1, rajaton) + kolme Tieriä sekä Mosswoodille että Luuluolalle, kukin omalla omistetulla määrällään ("LUULUOLA T2 (1 kpl)") tai harmaana tekstillä "Ei karttoja, tapa bosseja saadaksesi" kun 0 kpl; kuvaustekstit pudotettiin riveiltä tilan takia (7 riviä mahtuu pystysuunnassa vain ilman niitä). Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa `eval`-kutsuilla: MAPIT-tab kuvakaappauksella (aiemmista testeistä persistoituneet PlayerPrefs-arvot Mosswood T1=6 ja Luuluola T1=5 näkyivät oikein sekaisin uusien Luuluola T2/T3-appujen kanssa, todistaen tallennus säilyy Tier-avaimittain erillään); valittiin Luuluola T3, varmistettiin `ConsumeMapItem` vähensi juuri T3:sta (5→5, 1→1, 0→0 ei muuttunut muille Tiereille, T3 1→0) eikä muihin Luuluola-Tiereihin koskettu, ja `MapTierMultiplier`=1,7 täsmäsi kaavaan; spawnattiin vihollinen suoraan `SpawnEnemy()`:illä ja mitattiin health≈60,1 (odotettu 34×1,7≈57,8, ero Elapsed-ajasta) - vertailuksi vaihdettiin Mosswood T1:lle ja sama spawni antoi tasan health=34 (kerroin 1,0) - mitattavasti erilainen, kuten pyydettiin. Droppijakauma varmennettu 3000 `TryDropMapItem()`-kutsun otoksella: 17 % droppasi (tavoite 18 %), Tier-jakauma droppien sisällä T1 63,3 %/T2 25,9 %/T3 10,8 % (tavoite 60/30/10, riittävän lähellä 3000 kutsun otoskoolla), Mosswood/Luuluola-jako pysyi tasan (250/260). Konsolissa ei uusia virheitä paitsi tuttu, jo aiemmin dokumentoitu NetworkObject-siivousvaroitus muutaman `SpawnEnemy()`/`ExitToMainMenu`-testikutsun jäljiltä - `world`/`player` eivät olleet null, peli toimi normaalisti heti perään, ei koodivirhe. **Committattu nykyisessä muodossaan** (sama väliaikainen-palautus-menettely kuin aiemmissakin MAPIT-committeissa, koska `SurvivorProceduralMaps.cs` sisälsi jälleen samaan aikaan toisen istunnon `BuildInfernoBossArena()`/drop-imu-rivejä - palautettiin HEAD:iin ennen commitointia ja takaisin heti perään, diff täsmäsi tarkalleen).

- 2026-09-09: **Per-hahmo ase-/taito-omistus + kauppa, HAHMOT-tab käyttöön, ja Mappi-tilan loadout korvattu omistuspohjaisella versiolla.** Selviytymistila (proseduraalikartta) pysyy täysin ennallaan koko tämän merkinnän ajan - kaikki alla vaikuttaa vain kun `activeMapType != MapType.Procedural`.
  - Uusi tiedosto `CharacterShop.cs` (`AccountProgression.cs`:n malliin): `Dictionary<PlayerCharacter, HashSet<int>> ownedUpgrades` (samat upgrade-id:t kuin `UpgradeNames`/`mapLoadout` käyttävät). Jokaisen hahmon oma kiinteä aloitusase (esim. Velho→Lightning, Ritari→Sword, ks. `SelectCharacter` `SurvivorPottu.cs`:ssä) on aina omistettuna oletuksena, lisätään joka `LoadCharacterShop()`-kutsulla eikä koskaan menetetä. Potulla ei ole Selviytymistilassa kiinteää asetta (vapaa 30 aseen valinta), joten Mappi-tilan oletusomistukseksi valittiin Sword - mielivaltainen mutta järkevä valinta, jotta loadout-poolissa on aina vähintään yksi ase tuoreellakin Potulla. Tallennus `PlayerPrefs`iin per hahmo (`SaveKey+"Owned_"+character`, pilkuin eroteltu id-lista). Hinta: `UpgradePrice(character) = 25 + omistettujen_määrä*20` (sama tyylinen kasvava kaava kuin `BuyLegacyHealth`:in "20 + LegacyHealth*10"). `BuyCharacterUpgrade()` tarkistaa jo-omistuksen ja Silverin, vähentää Silverin ja tallentaa. **Sivuhavainto/pieni korjaus**: `Silver` ladattiin aiemmin `PlayerPrefs`ista vasta `ResetProgression()`:issa (pelikerran alkaessa) - päävalikossa se olisi näkynyt aina nollana ennen ensimmäistä käynnistystä. `LoadCharacterShop()` (kutsutaan `Start()`:ista) lataa nyt `Silver`:in heti, jotta HAHMOT-tabin kauppa näyttää oikean saldon heti valikkoon tultaessa; `ResetProgression()`:in oma lataus säilyy ennallaan (uudelleenlataa saman arvon, harmiton).
  - HAHMOT-tab (`MenuScreen.Characters`, `DrawCharacterTab()` `CharacterShop.cs`:ssä) lisätty päävalikon navigaatioon KOTI:n ja MONINPELI:n väliin. Näyttää hahmovalitsimen (9 nappia), valitun hahmon nimen/Mastery-tason/Silverin, sekä ruudukon kaikista 30 aseesta (6×5) ja 9 Tomesta (3×3) - omistetut harmaina "(omistettu)"-tekstillä, muut ostettavina hintanappeina jotka `GUI.enabled=false` kun Silver ei riitä.
  - Mappi-tilan loadout-valinta (`DrawLoadoutSelect()`) kirjoitettu uusiksi käyttämään `OwnedUpgradesFor(SelectedCharacter)`:ia aiemman koko-30-aseen-roosterin sijaan - näyttää vain sen minkä valittu hahmo omistaa, ruudukko mukautuu rivimäärältään omistettujen määrään (ei enää kiinteät 5+3 riviä). **Valintajärjestys korjattu**: koska loadout riippuu nyt valitusta hahmosta, hahmovalinta (`awaitingCharacterChoice`) täytyy tapahtua ENNEN loadout-ruutua (`awaitingLoadoutChoice`) - vaihdettu `DrawProgression()`:in Selecting-haaran järjestystä. Tämä paljasti ongelman: `SelectCharacter()` (`SurvivorPottu.cs`) päätti aiemmin heti ei-Pottu-hahmoille `Selecting=false` (ohittaen koko lopun Selecting-sekvenssin), mikä olisi estänyt loadout-ruudun näkymisen kokonaan Velholle/Ritarille/jne. - korjattu ehdolla `character!=PlayerCharacter.Pottu && !awaitingLoadoutChoice`, ja uusi `FinishLoadoutSetup()`-apumetodi (kutsutaan `ConfirmLoadout()`/`SkipLoadout()`:ista `BeginMapRun()`/`CreateLandmarks()`:in lisäksi) päättää `Selecting=false`:n ei-Pottu-hahmoille vasta kun loadout on ratkaistu. `BuildUpgradePool()`/`SelectStarter()`/`DrawStarterPages()` (rajaus valittuun `mapLoadout`:iin) eivät tarvinneet lisämuutoksia - ne toimivat jo ennallaan riippumatta siitä mistä `mapLoadout` täytettiin. Jos hahmolla on omistettuna alle 5+5, loadout näyttää vain sen minkä omistaa eikä virhettä tyhjästä poolista tule - OHITA-nappi (nimetty uudelleen "OHITA (käytä koko omistetusta poolista)") käyttää koko omistetun poolin turvallisena minimipoolina, sisältäen aina vähintään sen kiinteän oletusaseen. Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa `eval`-kutsuilla: Silver (149) näkyi HAHMOT-tabissa heti valikkoon tultaessa ilman että run oli käynnistetty; ostettiin Potulle Bow (45g) ja Lightning (65g) - Silver väheni oikein (149→104→39), kolmas osto (Chunkers, 85g) epäonnistui odotetusti riittämättömän Silverin takia (`bought=False`, Silver muuttumaton), kuvakaappaus vahvisti "(omistettu)"-tilan Pannu/Bow/Lightning-napeissa ja harmaat/pois-päältä-napit lopuille (85g > 39 Silver). Mappi-tila-run Potulla näytti loadoutin oikein 3 omistetulla aseella (0 Tomea), valittiin kaikki 3 ja VAHVISTA vei `DrawStarterPages`:iin joka näytti "1/1"-sivun vain näillä kolmella; `BuildUpgradePool()` antoi tasan `[0,1,2]`. Testattiin myös ei-Pottu-hahmo (Velho, vain Lightning omistettuna): loadout-ruutu näytti oikein vain Lightningin eikä hypännyt suoraan peliin (`Selecting` pysyi `True` `SelectCharacter()`:in jälkeen); OHITA-nappi päätti `Selecting=false`:n oikein ja peli käynnistyi Luuluolalla kuvakaappauksella vahvistettuna (Velho taistelee normaalisti). Selviytymistila varmistettu lopuksi täysin muuttumattomaksi: `awaitingLoadoutChoice`=false koko ajan, `BuildUpgradePool()` antoi täyden 39 kohteen poolin riippumatta hahmon `ownedUpgrades`-omistuksesta. Ei konsolivirheitä paitsi tuttu NetworkObject-siivousvaroitus `ExitToMainMenu`-kutsujen jäljiltä (jo aiemmin dokumentoitu, ei liity tähän muutokseen). **Committattu ase-varianttityön yhteydessä** (ks. alla oleva 2026-09-09-merkintä "Ase-variantit") - variantit riippuvat suoraan tästä (`IsUpgradeOwned`, `DrawLoadoutSelect`), joten kumpikaan ei toiminut/kääntynyt itsenäisesti ilman toista.

- 2026-09-09: **Aloitettu loadout-systeemi Mappi-tilaan (väliversio, korvattu yllä olevalla per-hahmo-omistus-versiolla).** Koska ase-/Tome-varianttien ja fragmenttien avausjärjestelmää ei ole vielä olemassa, pelaaja valitsee toistaiseksi vapaasti enintään 5 asetta ja 5 Tomea (`MaxWeaponKinds`/`MaxTomeKinds`) koko nykyisestä 30 aseen + 9 Tomen valikoimasta - **täysi "vain avatut" -loadout-versio tulee myöhemmin, kun ase-/Tome-variantit on toteutettu**. Uusi `DrawLoadoutSelect()` (`SurvivorProgression.cs`) näyttää kaikki 30 asetta (6×5-ruudukko) ja 9 Tomea (3×3-ruudukko) valintanappeina laskureineen ("ASEET (3/5)"), sekä VAHVISTA- ja OHITA-napit. Näytetään `awaitingLoadoutChoice`-lipun ohjaamana Selecting-sekvenssin alussa, ennen hahmovalintaa - lippu asetetaan `ResetProgression()`:issa sen mukaan onko `selectedMapType != MapType.Procedural` (Selviytymistilassa aina false, ei muutu miltään osin). Koska `BeginMapRun()`/`CreateLandmarks()` kutsuttiin aiemmin synkronisesti heti `ResetProgression()`:issa ilman että pelaaja ehti nähdä mitään UI:ta välissä, niiden kutsu siirrettiin pois `ResetProgression()`:ista `ConfirmLoadout()`:iin/`SkipLoadout()`:iin (kutsutaan vasta kun pelaaja painaa VAHVISTA tai OHITA) - turvallista, koska kiinteillä kartoilla `MapSpawn` on aina `Vector3.zero`-fallback riippumatta siitä onko kartta jo rakennettu, joten viive ei riko pelaajan spawn-sijaintia. `BuildUpgradePool()` (level-up-tarjonta) rajattu `mapLoadoutActive`:n ollessa päällä vain valittuihin id:ihin, MUTTA jo omistettu ase/Tome pysyy aina tarjolla leveloitavaksi vaikka se ei olisi valitussa poolissa (turvaverkko hahmokohtaisille automaattisille aloitusaseille, esim. Velhon Salamasauva - ks. tunnettu rajoitus alla). `SelectStarter()` hylkää poolin ulkopuolisen aseen (`return false`) kun `mapLoadoutActive`; `DrawStarterPages()` (`SurvivorArsenal.cs`) suodattaa näytettävät sivut samaan pooliin niin ettei pelaaja edes näe valitsemattomia aseita eikä sivutus mene rikki (paginoi suodatetun listan yli, ei enää suoraan `Weapon`-indeksin yli). Tyhjä VAHVISTA (0 valintaa) käyttäytyy kuin OHITA (`mapLoadoutActive = mapLoadout.Count > 0`) - run ei voi jäädä jumiin tyhjällä poolilla. **Tunnettu rajoitus (tarkoituksella, ei pyydetty laajemmin)**: muiden hahmojen kuin Potun kiinteät aloitusaseet (esim. Velho/Salamasauva, Ritari/Sword) eivät kulje loadout-valinnan kautta ollenkaan - `SelectCharacter()`:ia ei muutettu, joten ne asettuvat suoraan `weaponLevels`:iin kuten ennenkin ja pysyvät levelöitävissä edellä mainitun "jo omistettu" -turvaverkon ansiosta, mutta eivät kuluta loadout-ruudun 5 asepaikkaa eivätkä näy siinä valittuina. Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa `eval`-kutsuilla: Selviytymistila (Procedural) ohitti loadout-ruudun kokonaan (`awaitingLoadoutChoice`=false koko ajan) ja `BuildUpgradePool()` antoi täyden 39 kohteen poolin kuten ennen muutosta - ei muutosta. Mappi-tila-run (Luuluola T1) näytti loadout-ruudun ensimmäisenä kuvakaappauksella (kaikki 30 asetta + 9 Tomea näkyvissä); testattiin 5+5-katto yli-valinnalla (7 yritystä molemmissa, pysähtyi tasan 5:een), VAHVISTA rakensi kartan (`MapLayout`=null, kiinteä kartta) ja siirsi hahmovalintaan, `DrawStarterPages` näytti sivutuksen "1/2" (5 asetta/3 per sivu) vain valituilla aseilla oikeine nimineen molemmilla sivuilla; `BuildUpgradePool()` antoi tasan 10 kohdetta (5 valittua asetta + 5 valittua Tomea, ei yhtään muuta) - varmennettu myös 500 toiston otoksella ettei poolin ulkopuolinen id koskaan esiinny; `SelectStarter()` hylkäsi poolin ulkopuolisen aseen (Bone, kun loadout=vain Pannu) ja hyväksyi poolin sisäisen; OHITA-nappi testattu erikseen omalla runilla - `mapLoadoutActive`=false, täysi 39 kohteen pooli, kartta silti rakentui normaalisti. Ei konsolivirheitä paitsi tuttu NetworkObject-siivousvaroitus `ExitToMainMenu`-kutsujen jäljiltä (jo aiemmin dokumentoitu, ei liity tähän muutokseen). **Committattu ase-varianttityön yhteydessä** (ks. alla oleva 2026-09-09-merkintä "Ase-variantit"), korvattuna yllä olevalla per-hahmo-omistus-versiolla.

- 2026-09-09: Toteutettu Mappi-inventaario MAPIT-tabiin, karttojen droppausjärjestelmän pohjaksi. Uusi tiedosto `MapInventory.cs` (`AccountProgression.cs`:n mallia noudattaen): `Dictionary<(MapType type, int tier), int> mapItems`, Tier kiinnitetty toistaiseksi arvoon 1 mutta rakenne (type,tier)-avaimella tukee useampaa Tieriä myöhemmin ilman uudelleenkirjoitusta. Tallennus `PlayerPrefs`iin erillisillä avaimilla (`SaveKey+"MapItem_"+type+"_"+tier`), samaan tapaan kuin Silver/AccountXP. `LoadMapInventory()` kutsutaan `Start()`:ista (`SurvivorGame.cs`). Proseduraalinen kartta pysyy ilmaisena/rajattomana - sille ei koskaan lasketa eikä näytetä `mapItems`-määrää. Droppilähde: `Hit()`:in bossinkaatohaara (`SurvivorGame.cs`, sama kohta joka jo käynnistää `BeginMapTransition()`:in ja kasvattaa `bossKills`:ia) kutsuu uutta `TryDropMapItem()`:ia - 18 % mahdollisuus, satunnaisesti Mosswood tai Luuluola (ei Procedural), näyttää `notice`-ilmoituksen ("Löysit Karttaesineen: X!") samalla mekanismilla kuin muuallakin (ylikirjoittaa hetkeksi `BeginMapTransition()`:in "Bossi voitettu"-viestin jos droppi osuu - hyväksytty, tarkoituksella jätetty yksinkertaiseksi). MAPIT-tab (`DrawMapTypeTab()`/`DrawMapTypeOption()`, `SurvivorProceduralMaps.cs`) näyttää nyt omistetun määrän jokaisen kiinteän kartan kohdalla ("MOSSWOOD (2 kpl)"), ja `GUI.enabled=false` + "Ei karttoja - tapa bosseja saadaksesi" kun määrä on 0 (Procedural aina päällä, merkitty "Rajaton"). `BeginMapRun()` kuluttaa yhden kappaleen (`ConsumeMapItem`) vasta pelikerran alkaessa, ei valintahetkellä selailussa - jos valittu kartta on ehtinyt loppua (esim. käytetty muualla), pudotaan turvallisesti proseduraaliseen sen sijaan että pelaaja jäisi jumiin tai peli kaatuisi, ja `selectedMapType` synkataan takaisin todelliseen `activeMapType`:hen niin ettei MAPIT-tab jää näyttämään harhaanjohtavasti valituksi jotain mitä ei enää omisteta. Ei Tier-skaalausta, fragmentteja eikä sustain-resurssia - pelkkä "droppaa → omista → kuluta pelatessa", kuten pyydettiin. Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa: MAPIT-tab kuvakaappauksella alussa (molemmat 0 kpl, napit harmaina/pois päältä, oikea teksti), pakotettu inventaario `eval`-kutsuilla (Mosswood 2, Luuluola 1) ja kuvakaappaus vahvisti napit aktivoituvan ja määrät näkyvän oikein; valittiin Luuluola ja varmistettiin `MapItemCount` pysyy muuttumattomana ennen PELAA-painallusta ja vähenee tasan yhdellä juuri `BeginMapRun()`:issa (1→0, `activeMapType`=BoneCave), kartta latautui oikein kuvakaappauksella. Droppitodennäköisyys varmennettu tilastollisesti kahdella tavalla: suoralla `TryDropMapItem()`-kutsulla 200 kertaa (33/200 ≈ 16,5 %, odotettu 18 %) ja oikealla pelimekaniikalla - luotiin 80 oikeaa bossia ja tapettiin ne `Hit()`:in kautta (9/80 ≈ 11,3 %, tilastollisesti uskottava n=80:lla) - molemmissa Mosswood/Luuluola-jako pysyi tasapainossa eikä konsoliin tullut yhtään uutta virhettä. Varasuoja testattu tyhjentämällä molemmat `ConsumeMapItem`-silmukalla nollaan asti (MAPIT näytti oikein "Ei karttoja"), minkä jälkeen PELAA valitulla-mutta-tyhjällä Luuluolalla pudotti siististi proseduraaliseen (`activeMapType`/`selectedMapType` molemmat `Procedural`, `MapLayout` ei null) ilman virhettä. Yhden testivaiheen aikana (peräkkäiset `ExitToMainMenu`/`ConsumeMapItem`-silmukka/`StartGame`-kutsut nopeasti eval:lla) konsoliin ilmestyi hetkellinen purske `OnGUI`/`UpdateCamera`-NullReferenceExceptioneja ja Netcode-dispose-virheitä (sama aiemmin dokumentoitu CLI-ajoitusongelma kuin 2026-09-08-merkinnässä, ei toistunut `recompile`-kutsua tällä kertaa) - tila korjaantui itsestään seuraavaan frameen mennessä (`world`/`player` eivät olleet null, peli toimi normaalisti heti perään), ei koodivirhe. Ei committia vielä tälle osalle.

- 2026-09-09: Committattu Luuluola-kartta (`SurvivorBoneCaveMap.cs`) sellaisenaan (ks. alempi 2026-09-09-merkintä), ja kytketty MAPIT-tab oikeasti toimimaan. `SurvivorProceduralMaps.cs`: uusi `enum MapType { Procedural, Mosswood, BoneCave }` sekä `selectedMapType` (MAPIT-tabin näyttämä/muokkaama valinta) ja `activeMapType` (lukitaan `BeginMapRun()`:issa pelikerran alkaessa, joten tabin vaihtaminen kesken pelin ei vaihda karttaa jalkojen alta). `DrawMapTypeTab()` näyttää kolme nappia (SATUNNAINEN/oletus, MOSSWOOD, LUULUOLA) lyhyine kuvauksineen, valittu korostettuna. Uusi `BuildFixedMap(MapType)` tekee saman siivouksen kuin `GenerateProceduralMap()` (tuhoaa vanhan proseduraali- tai kiinteän karttajuuren, tyhjentää `mapMeshes`/`proceduralMaterials`/`forestObstacles`) mutta kutsuu `BuildForestMap()`/`BuildBoneCaveMap()`:ia ja jättää `MapLayout`:in nulliksi - tämä on jo valmiiksi peliin tallella oleva "legacy" (esi-proseduraalinen) reitti: `ClampToMap`/`ResolveMapPosition`/`MoveOnMap` (`SurvivorForestMap.cs`) ja `CreateLandmarks()`/bossin sijoitus (`SurvivorProgression.cs`) tarkistavat jo kaikki `MapLayout == null` ja lankeavat kiinteän kartan `arenaRadius`-suhteutettuihin sijainteihin, joten mitään uutta null-suojausta ei tarvinnut lisätä. `BeginMapRun()` (ensimmäinen alue) ja `AdvanceArea()` (alueen vaihto) haarautuvat molemmat `activeMapType`:n mukaan, joten valittu kiinteä kartta pysyy samana koko pelikerran ajan (ei vaihdu proseduraaliseksi alueella 2+). Jos MAPIT-tabista ei valita mitään, `selectedMapType` pysyy oletuksena `Procedural` eikä käyttäytyminen muutu lainkaan. Ei Tier-skaalausta eikä drop-systeemiä - pelkkä kartanvalinta, kuten pyydettiin. **Tunnettu rajoitus (ei korjattu, ei pyydetty)**: `SurvivorGameNetwork.cs`:n moninpelin karttasynkronointi (`GenerateProceduralMap(seed,false)` liittyvälle) ei tunne `MapType`:a - jos isäntä pelaa kiinteällä kartalla ja kaveri liittyy, kaverin kartta ei todennäköisesti synkkaa oikein; ei testattu eikä kuulunut tämän tehtävän piiriin. Varmennettu Unity CLI:llä: käännös 0 virhettä. Play Modessa käytiin läpi koko sykli `eval`-kutsuilla (koska OnGUI-valikko ei näy `source=screen`-kuvakaappauksessa pelin sisällä, karttaa itseään tarkasteltiin `source=camera`-kuvilla): proseduraalinen → Mosswood → Luuluola → takaisin proseduraaliseen, jokaisen kohdalla `StartGame`→hahmo/asevalinta→muutaman sekunnin taistelu→`ExitToMainMenu`, sekä MAPIT-tabin UI tarkistettiin kuvakaappauksella (LUULUOLA-nappi korostui oikein valittuna). Jokaisessa vaiheessa `MapLayout`-nullius täsmäsi odotettuun, vanha karttajuuri tuhoutui ennen uuden rakentamista, chestit/pyhäkkö/portaali ja viholliset toimivat kaikilla kolmella kartalla eikä konsoliin tullut yhtään uutta virhettä koko syklin aikana (staattinen NetworkManagerin Netcode-dispose-virhe toistuu edelleen pelkästä Play Moden pysäyttämisestä/uudelleenkäynnistyksestä, aiemmin dokumentoitu, ei liity tähän muutokseen - eristettiin erikseen toistamalla puhdas proseduraali-`ExitToMainMenu` ilman karttavaihtoa: ei virhettä). Muutosloki päivitetty, ei committia tälle osalle vielä.

- 2026-09-09: Lisätty uusi kiinteä Map Type -karttaehdokas "Luuluola" (`Assets/Survivor/Scripts/SurvivorBoneCaveMap.cs`, `BuildBoneCaveMap()`), `SurvivorForestMap.cs`:n (`BuildForestMap()`/Mosswood) rakennetta mallina käyttäen. Kumpikaan ei ole enää oikeasti pelin oletusreitillä - `BuildWorld()` kutsuu nykyään `GenerateProceduralMap()`:ia (proseduraalinen luolageneraattori, `SurvivorProceduralMaps.cs`), ja jokainen `ResetRun()`/`AdvanceArea()` generoi uuden proseduraalisen kartan joka tapauksessa - joten Mosswood ja Luuluola ovat tällä hetkellä käyttämättömiä, mutta valmiita "kiinteitä" Map Type -vaihtoehtoja tulevaa valintaruutua varten, samoja jaettuja liikkumisfunktioita (`ClampToMap`/`ResolveMapPosition`/`MoveOnMap`, `ForestOutline`, `forestObstacles`) käyttäen. Luuluola: harmaa/luuvalkoinen maasto (kivilattia `#9E9A8C`, tomulaikut vaaleampana), pystyt hautakivimäiset esteet (kapea, hieman kapeneva laattamainen `FacetedMesh`, satunnaisella pienellä kallistuksella) kivien/mäntyjen sijaan sekä sama hautakivirivi kartan reunalla kiviradan/mäntyjen sijaan. Käyttää kiinteää `System.Random(50301)`-siementä (jäädytetty, ei satunnainen joka pelikerralla) samaan tapaan kuin Mosswoodin `81427`. Ei Map Type -valintaruutua eikä kytköstä MAPIT-tabiin - pelkkä toinen kiinteä kartta olemassa olevan rinnalle. Varmennettu Unity CLI:llä: käännös 0 virhettä, testattu Play Modessa väliaikaisesti vaihtamalla `BuildWorld()`:in proseduraalikutsu `BuildBoneCaveMap()`-kutsuksi (koska `ResetRun`/`AdvanceArea` generoisivat proseduraalikartan päälle heti PELAA-napista, karttaa ei voinut katsoa normaalin pelin sisällä - pelaaja/kamera siirrettiin sen sijaan suoraan `eval`-kutsuilla Luuluolan päälle `capture_game_view(source=camera)`:lla, koska `source=screen` näyttää vain päävalikon OnGUI-tummennuksen), kuvista näkyi oikea harmaa maasto, tomulaikut, pystyt hautakivet ja reunan hautakivirivi - ei konsolivirheitä. `BuildWorld()` palautettu takaisin `GenerateProceduralMap(81427,false)`-kutsuun testin jälkeen. Ei committia vielä tälle osalle.

- 2026-09-09: Päävalikkoon (`SurvivorGame.cs`) lisätty yksinkertainen välilehtinavigaatio korvaamaan aiempi `settingsOpen`-bool ja kiinteä `DrawMultiplayerPanel()`-lohko. Uusi `enum MenuScreen { Home, Multiplayer, Maps, Leaderboards, Settings }` (poistettu `settingsOpen`) ja `DrawMenuTabs()`-välilehtirivi `DrawMainMenu()`:in yläreunaan (KOTI, MONINPELI, MAPIT, TULOSTAULUT, ASETUKSET). Alkuperäinen pyyntö sisälsi myös HAHMOT-välilehden hahmovalinnalle, mutta käyttäjältä kysyttäessä kävi ilmi, että `DrawCharacterSelect()`/`DrawLegacy()` eivät tällä hetkellä ole osa päävalikkoa lainkaan - ne näytetään vasta PELAA-napin painamisen jälkeen (`Selecting && awaitingCharacterChoice`, `AtMainMenu` on silloin jo false). Käyttäjä päätti jättää HAHMOT-tabin ja koko hahmovalinnan päävalikkoon tuomisen (isompi pelin kulun muutos) omaksi myöhemmäksi tehtäväkseen, kun sillä on oikeaa sisältöä (esim. Mastery-kosmetiikka) - hahmovalinta säilyy täysin ennallaan. KOTI-tabille jäi entinen päävalikon sisältö (otsikko, PELAA, LOPETA, karttasiemen-valinta) miinus erillinen ASETUKSET-nappi (korvautui tabilla). MONINPELI-tab käyttää samaa `DrawMultiplayerPanel()`-sisältöä uudessa sijainnissa (samat toiminnot: isännöi/liity/kopioi koodi). MAPIT ja TULOSTAULUT ovat `GUI.enabled = false` -platseholdereita tekstillä "Tulossa myöhemmin". ASETUKSET-tab säilytti FPS-valinnan ja äänenvoimakkuuden identtisenä, poistaen vain tarpeettoman TAKAISIN-napin (tabien klikkaus korvaa sen). Varmennettu Unity CLI:llä: käännös 0 virhettä, Play Mode -testi jossa jokainen viidestä tabista käytiin läpi `eval`-kutsuilla (koska OnGUI ei näy pelkässä kamerakuvassa, tila vaihdettiin reflektiolla ja tarkistettiin `capture_game_view(source=screen)`-kuvilla) - kaikki näyttivät oikein eikä konsoliin tullut uusia virheitä testin aikana. Ei committia/pushia vielä.

- 2026-09-09: Vaihdettu kapselibossi Feyloomin Skeleton Necromancerin URP-malliin ja mukana tulleeseen sauvaan (`Resources/SkeletonNecromancerBoss.prefab`). Paketin FBX:ssä ei ole animaatioleikkeitä (importAnimation=true, defaultClipAnimations ja LoadAllAssetsAtPath-leikkeet tyhjiä), joten kelvolliselle humanoid-avatarille kytkettiin projektin UndeadEnemy-controllerin idle/walk/attack-animaatiot väliaikaiseksi ratkaisuksi. Bossin shokkiaalto laukaisee Attack-animaation. Bossiportaalin ympärille generoidaan Inferno World Free -paketin tasanteesta, pylväistä, tulimaljoista, kivistä ja patsaista areena, jossa on basalttilattia ja koristeellinen laavaura. Assettiviitteet tallennettu Resources/InfernoBossAssets.asset-tiedostoon, joten toimivat myös buildissa. Koristeilla ei ole fysiikkatörmäyksiä; nykyinen navigointiruudukko säilyy. Varmennettu Unity CLI:llä: käännös 0 virhettä, pelinäkymän kuvallinen tarkistus, portaalin Interact-kutsu oikeasta sijainnista, Walk/Attack-klipit sekä bossin kuolema ja alueenvaihto 1→2 uuden areenan kanssa. Pelitestissä 0 konsolivirhettä; Play Modesta poistuttaessa ennestään dokumentoidut Netcode Dispose -virheet. Ei buildia eikä julkaisua. Paketin omat animaatiot jäävät odottamaan niiden tuontia.

- 2026-09-08: Julkaistu v0.3.5 (edellinen: v0.3.4). Commitoitu ja pushattu ennen buildia kaksi committia: Account Level + Mastery -taustalogiikka (`AccountProgression.cs` ym., ks. alempi 2026-09-08-merkintä) ja tämän testauskierroksen UI-korjaukset (puuttuva `badgeStyle`, Mastery-badgen päällekkäisyys pitkien nimien kanssa). Buildattu Unity CLI:llä (StandaloneWindows64, 0 virhettä, 6 samaa jo dokumentoitua varoitusta), zipattu ja liitetty GitHub-releaseen [v0.3.5](https://github.com/Marcebooy/SurvivorPrototype/releases/tag/v0.3.5). Käyttäjän pyynnöstä versionumero v0.3.5 eikä v0.3.4, koska v0.3.4 oli jo julkaistu aiemmin samana päivänä. **Discord-ilmoitusta ei lähetetty käyttäjän pyynnöstä.**

- 2026-09-08: Testattu toisen istunnon lisäämä Account Level + Mastery -UI (uusi `badgeStyle`, `SurvivorPottu.cs: DrawCharacterCard` - Mastery-badge kortin oikeassa yläkulmassa, `SurvivorProgression.cs: DrawLegacy` - "Account Lv." Silverin vieressä, rivin leveys 800→900). **Käännös epäonnistui aluksi**: `badgeStyle`-muuttujaa käytettiin `SurvivorPottu.cs`:ssä, mutta sitä ei koskaan lisätty `SurvivorGame.cs`:n style-kenttiin eikä alustettu `OnGUI()`:n style-lohkossa (`CS0103: The name 'badgeStyle' does not exist`) - lisätty puuttuva kenttä ja alustus (keltainen, oikealle tasattu, fontSize 13) korjauksena, jotta pyydetty testaus ylipäätään pääsi käyntiin. **Toinen bugi löytyi geometrisella tarkistuksella**: koska Unity CLI:n `screenshot`/`capture_game_view` ei tässä ympäristössä koskaan sisällä OnGUI-kerrosta (vain kameran renderöinnin, todennettu kolmella eri pelitilanteen kuvakaappauksella - pelkkä Pottu-hahmo maastossa ilman mitään HUD:ia tai valikkoa niissäkin kohdissa missä `Selecting`/`AtMainMenu` oli varmistetusti eval:lla oikeassa tilassa valintaruudun näyttämiseksi), visuaalista "menee päällekkäin nimen kanssa" -tarkistusta ei voitu tehdä kuvasta - sen sijaan mitattiin `GUIStyle.CalcSize` suoraan `eval`-kutsulla jokaiselle yhdeksälle hahmonimelle: alkuperäisellä `hudNameStyle`illä (fontSize 21, ei clippausta, nimikenttä x+14..width-92) NECROMANCER (165,7 px) ja METSÄSTÄJÄ (141,8 px) ylittivät badgen alkukohdan (129,2 px nimikentän alusta) 36,5 px ja 12,6 px - juuri käyttäjän epäilemät kaksi pisintä nimeä olisivat menneet badgen alle. Korjattu uudella `cardNameStyle`illä (kloonattu `hudNameStyle`:stä, fontSize 17 + `TextClipping.Clip` turvaverkoksi) ja kaventamalla nimikenttä täsmälleen badgen alkuun asti (width-92 → width-84, ei enää omaa aluetta joka ulottuisi badgen puolelle edes clipin kanssa) - fontSize 17:llä laskettuna kaikki 9 nimeä mahtuvat uuteen 143,2 px kenttään reilulla marginaalilla (leveimpänä NECROMANCER 134,1 px). Varmennettu Play Modessa `eval`-kutsuilla: pelattiin yksi run läpi kokonaan (`FinishRun()` Kills=80/bossKills=3/Elapsed=400s), AccountXP/Level ja Pottu-hahmon MasteryLevel kasvoivat oikein (228→498 XP, Mastery 2→4) ja näkyivät oikein Legacy-paneelissa uuden runin valintaruudussa; PlayerPreferillä pakotettiin lisäksi Necromancerin ja Metsästäjän Mastery-taso keinotekoisesti 38:aan (kaksinumeroinen badge, pahin tapaus) ja avattiin hahmovalintaruutu - ei yhtäkään OnGUI-poikkeusta konsolissa kummallakaan pistetarkistuksella. **Sivuhavainto testauksesta (ei liity tähän muutokseen)**: kesken testin `recompile`-kutsu osui käynnissä olevaan Play Mode -istuntoon ja aiheutti hetkellisen tilan rikkoutumisen (`player`/`world`-viittaukset nollautuivat, `Update`/`OnGUI`/`UpdateCamera` heittivät NullReferenceExceptionia joka framella) - korjaantui itsestään puhtaalla Play Mode -pysäytyksellä/uudelleenkäynnistyksellä, ei koodivirhe vaan CLI-työkalujen ajoitusongelma tässä istunnossa; ei toistunut kun Play Mode käynnistettiin puhtaalta pöydältä ennen testien ajoa. Ei committia/pushia - käyttäjän pyynnöstä.

- 2026-09-08: Testattu toisen istunnon lisäämä Account Level + Mastery -pohja (`AccountProgression.cs`: `AccountXP`/`AccountLevel` koko tilille, `MasteryXpFor`/`MasteryLevelFor` per hahmo max 100, `GrantAccountAndMasteryXp()` kutsuttu `FinishRun()`:sta ennen Silver-palkintoa). Kolme tiedostoa kääntyivät virheittä (`recompile`, 0 virhettä), `.meta` syntyi automaattisesti importissa. **Testissä löytyi ja korjattiin bugi**: `accountXpPaidThisRun`-lippu nollattiin vain kentän alustuksessa, ei koskaan `ResetProgression()`:ssa (uuden runin alussa) - käytännössä XP olisi myönnetty vain kertaalleen per peli-istunto, kaikki sitä seuraavat runit olisivat menettäneet Account-/Mastery-XP:n hiljaa (Silver-palkinto ei kärsinyt, koska `rewardPaid` nollautui jo oikein). Lisätty `accountXpPaidThisRun = false;` `ResetProgression()`:iin (`SurvivorProgression.cs`) rewardPaidin viereen. Varmennettu Unity CLI:n `eval`-kutsuilla reflektiolla (`Kills`/`Elapsed`/`bossKills`-arvot asetettu suoraan, `FinishRun()` ja `ResetProgression()` kutsuttu käsin, koska täyttä pelikierrosta ei ollut käytännöllistä ajaa inputilla): ensimmäinen simuloitu run (Kills=47, bossKills=1, Elapsed=245s) kasvatti AccountXP:n ja MasteryXP:n 0:sta 50:een ensimmäisessä testissä bugin kanssa, korjauksen jälkeen kaksi peräkkäistä runia (50→171→211) kasvattivat molempia oikein jokaisella runilla; PlayerPrefs-arvot (`AccountXP`, `MasteryXP_Pottu`) säilyivät oikein myös Play Moden pysäytyksen/uudelleenkäynnistyksen yli. Toistettu `FinishRun()`-kutsu samalla runilla pysyi no-oppina molemmissa (rewardPaid/accountXpPaidThisRun). Silver-palkintologiikka toimi koko ajan muuttumattomana rinnalla (29→35 Silver kahdella runilla). Ei uusia pelikoodiin liittyviä konsolivirheitä (vain jo dokumentoidut Netcode-tuhoamis-NRE:t Play Modesta poistuttaessa). Ei committia/pushia, ei Discord-ilmoitusta - käyttäjän pyynnöstä vasta ensimmäinen testauskierros.

- 2026-09-08: Damage-numerot verkotettu (isäntä lähettää `HostAvatarNet.ShowDamageNumberRpc`, kaveri piirtää oman paikallisen kopionsa - ks. `SurvivorGameNetwork.cs: BroadcastDamageNumber/AddNetworkDamageLabel/DrawNetworkDamageLabels`). Käyttäjän raportoima "XP-pudokkeet eivät näy kaverille" -väite tutkittu uudelleen Unity CLI:n eval-työkalulla (v0.3.3:n koodilla): vihollisen tappaminen kaukana isännästä tuotti `DropNet`-olion jonka `NetworkObject.IsSpawned=True`, `IsSceneObject=False`, sama rakenne kuin viholliset (jotka käyttäjä on vahvistanut näkyvän kaverille) - palvelinpuolen tila näyttää täysin terveeltä, ei löytynyt koodivirhettä. En pystynyt todentamaan asiaa oikean toisen pelaajan näkökulmasta (buildattu .exe ei tue Pipeline-etäohjausta, joten en voinut kirjautua sisään sillä koneella) - **jää auki, tarvitaan käyttäjän vahvistus onko tämä yhä ongelma v0.3.4:ssä**. Sen sijaan käyttäjä tarkensi tarkoittavansa toisella "hyökkäysanimaatiot"-maininnalla nimenomaan **aseiden efektejä (nuolet, taikaiskut, räjähdykset)** - nämä eivät ole koskaan olleet verkotettuja lainkaan (varmistettu koodista: `CastArsenal`/`CastAdvanced`/`TickShots`/`TickAdvancedShots`/`TickAdvancedZones` luovat kaikki paikallisia `Shape()`-primitiivejä, ei yhtäkään `NetworkObject`ia) - tämä ei ole bugi vaan toteuttamaton ominaisuus, laajuudeltaan iso (30 asetta, hyvin erilaisia liike-/osumamalleja) - ei aloitettu tässä. Julkaistu v0.3.4 (vain damage-numero-korjaus), ei Discord-ilmoitusta.

- 2026-09-08: Korjattu käyttäjän löytämä puute: kaveri näki isännän mutta ei vihollisten animaatioita, ei XP/kulta/terveyspudokkeita maassa, eikä iskujen animaatioita. Kolme erillistä syytä, kolme korjausta: (1) **Viholliset**: `Animator.SetFloat/SetTrigger`-kutsut eivät koskaan olleet verkotettuja - `NetworkAnimator` lisätty `UD_demo_character.prefab`iin, synkkaa Animatorin tilan automaattisesti. (2) **Pudokkeet**: XP/kulta/terveyspallot olivat pelkkiä paikallisia primitiivejä, eivät koskaan `NetworkObject`eja - uusi `DropNet.cs`+prefab (väri `IsHealth`-NetworkVariablesta), käytössä vain kun `IsNetworkHost` (offline yksinpeli käyttää yhä alkuperäistä `Shape()`-primitiiviä muuttumattomana - varmistettu erikseen ettei `dropPrefab`in pelkkä olemassaolo muuta yksinpelin ulkoasua). (3) **Iskujen/liikkeen animaatiot**: `characterVisual.Swing/Bonk/Block/Shockwave` ja `Tick()` ajetaan vain isännän omalla koneella riippumatta siitä kenen puolesta (isäntä tai kaveri) - kumpikaan REMOTE-osapuoli ei koskaan nähnyt niitä. Uusi `TriggerRelayVisual`-kääre (`ICharacterVisual`) toistaa diskreetit laukaisimet RPC:llä (`RemotePlayerNet`/`HostAvatarNet`, molemmat `[Rpc(SendTo.NotServer)]`) jokaisen osapuolen omaan paikalliseen malliin; jatkuva kävelyanimaatio (`Tick`) ei tarvitse verkotusta - jokainen osapuoli päättelee sen paikallisesti jo replikoidusta sijaintimuutoksesta. Sivuhuomio korjattu samalla: `HostAvatarNet`in oma peili näkyi myös isännälle itselleen (kaksi päällekkäistä hahmoa) - isännän oma kopio piilottaa nyt renderoijansa (`IsServer`-tarkistus), vain muut osapuolet näkevät peilin. Testattu Unity CLI:llä Play Modessa: isännän 29 renderoijaa pois päältä omalla koneella, kaveri tappoi vihollisen virheittä (Sword/Lightning), pudokas ilmestyi oikeana `NetworkObject`ina (`IsSpawned=True`), yksinpelin regressio (5 tappoa, 0 `DropNet`-instanssia offline) puhtaana. Julkaistu v0.3.3, ei Discord-ilmoitusta.

- 2026-09-08: Korjattu käyttäjän v0.3.1:ssä löytämä bugi: kaveri ei nähnyt omaa hahmoaan eikä isäntää pelissä. Syy oli kaksiosainen: (1) Vaihe 2:n `Combatant`-refaktoroinnissa kaverin hahmomalli rakentui kahdesta eri, keskenään kilpailevasta järjestelmästä (host-simuloitu `current`-osoittimen kautta ajettu `BuildCharacterVisual` JA uusi `RemotePlayerNet`in NetworkVariable-pohjainen rakennus) samaan Transformiin - isäntä näki kaksi päällekkäistä mallia, kaveri ei nähnyt mitään koska hänen oma koneensa ei koskaan ajanut kumpaakaan oikeaan aikaan. Korjattu niin että vain `RemotePlayerNet.RebuildVisual()` rakentaa mallin (ajetaan jokaisella osapuolella itsenäisesti samasta replikoidusta hahmovalinnasta, ei itse verkota meshiä), ja se kytkeytyy suoraan isännän `Combatant.CharacterVisual`iin taisteluanimaatioita varten (`SurvivorPottu.cs: SelectCharacter` ohittaa oman mallinsa rakennuksen kun `current != hostState`). (2) Isännän oma pelaajahahmo ei ollut koskaan verkotettu mitenkään - kaveri ei voinut nähdä isäntää ollenkaan. Uusi `HostAvatarNet.cs`+prefab: kevyt `NetworkObject` jonka isäntä luo isännöinnin alkaessa ja jonka sijainnin/hahmovalinnan se peilaa joka ruudulla omasta `player`-Transformistaan (`TickNetworkCoop`); muut osapuolet rakentavat siitä saman deterministisen mallin paikallisesti täsmälleen samalla `BuildCharacterVisualOn`-mekanismilla kuin kaverin oma hahmo. Testattu Unity CLI:llä Play Modessa palvelin-omisteisella synteettisellä kaverilla: täsmälleen yksi lapsiobjekti kaverin transformissa hahmonvalinnan jälkeen (ei kaksoiskappaletta), `FriendState.CharacterVisual` oikein kytketty, isäntähahmon peili näytti oikean hahmon ja seurasi sijaintia, ase ampui edelleen virheittä. Julkaistu v0.3.2 (`gh release create`), ei Discord-ilmoitusta.

- 2026-09-08: Käyttäjän pyynnöstä proseduraaliset huoneet ja käytävät korvattu yhdellä laajalla avoimella ympäristöllä (generaattori v2). Seed muuttaa alueen orgaanista ulkoreunaa ja maamerkkien paikkoja. Sisäseiniä tai kapeita käytäviä ei ole; kivireuna on vain kartan ulkorajalla. Seedit, minimappi, vaikeusskaalaus ja bossin jälkeinen automaattinen kartanvaihto säilyvät. Vanhan v1-generaattorin seedit tuottavat nyt v2-geometrian. Rooms-kokoelma on yhteensopivuuden vuoksi jäljellä vain näkymättöminä maamerkkiankkureina.

- 2026-09-08: Julkaistu v0.3.1 (edellinen: v0.3.0). Buildattu Unity CLI:llä (unity-editor-mcp:n `build`-työkalulla, koska Editor oli jo auki eikä erillistä batch-prosessia voinut käynnistää samaan projektiin - `unity build`-CLI epäonnistui lisenssilukituksesta), StandaloneWindows64, 0 virhettä. Kaikki committamattomat muutokset (proseduraaliset huone-/metsäkartat, moninpelin vaihe 2) commitoitu ja pushattu ensin (commit `55b2c60`). **Discord-ilmoitusta ei lähetetty käyttäjän pyynnöstä.**

- 2026-09-08: Moninpelin vaihe 2: kaverille oma hahmovalinta, aseet ja levelijärjestelmä (aiemmin kiinteä vihreä kapseli/lähitaistelu). Koko ase-/progressiojärjestelmä (`SurvivorProgression.cs`, `SurvivorArsenal.cs`, `SurvivorAdvancedWeapons.cs`, ~800 riviä, 30 asetta) oli kovakoodattu yhden `SurvivorGame`-instanssin kenttiin, joten sitä ei parametrisoitu käsin ase kerrallaan - sen sijaan uusi `Combatant.cs` kokoaa kaiken pelaajakohtaisen tilan (statit, aseomistus, asepoolit, XP/level) yhdeksi olioksi, ja `SurvivorGame` pitää `current`-osoitinta joka näyttää joko isännän omaan (`hostState`, aina isännän omassa pelisilmukassa) tai hetkellisesti kaverin `Combatant`iin (vain kun isäntä simuloi kaverin vuoroa `TickNetworkCoop`issa). Kaikki ~30 aseen `this.damage`/`this.player`/`this.weaponLevels`-lukevat metodit toimivat nyt muuttumattomina kumman tahansa pelaajan tilalle - ei tarvinnut kirjoittaa yhtäkään ase-/lasku-riviä uudelleen. Isännän oma pelattavuus säilyi täysin ennallaan (varmistettu Play Modessa: hahmovalinta → asevalinta → viholliset kuolevat → XP/kolikot kertyvät, identtinen ennen/jälkeen). Uudet julkiset apurit (`SurvivorGameNetwork.cs`): `CreateFriendCombatant`, `FriendSelectCharacter`, `FriendSelectStarter`, `FriendChooseUpgrade` (kaikki vaihtavat `current`in väliaikaisesti try/finally-lohkossa). Kaveri lähettää valinnat NGO 2.x:n uusilla `[Rpc(SendTo.Server)]`-metodeilla (`RemotePlayerNet.cs`: `RequestCharacterRpc`/`RequestStarterRpc`/`RequestUpgradeRpc` - projektin ensimmäinen RPC-käyttö, aiempi karttasiemenen synkka käytti `CustomMessagingManager`-viestejä) koska asiakas voi rakentaa koko valintaruudun (hahmokortit, asekaappi, level-up-vaihtoehdot) suoraan jaetusta staattisesta datasta (`CharacterRoster`, `UpgradeNames`/`UpgradeDetails`) ilman verkkoliikennettä - vain itse valinta ylittää verkon. Kaverin HP/Level/Coins/PendingChoices/Choices/Selecting-tila peilataan takaisin asiakkaalle uusilla `RemotePlayerNet`in `NetworkVariable`eilla joita isäntä päivittää joka `TickNetworkCoop`-tikillä. Kaveri kerää nyt myös XP/kulta/terveyspurkkeja pudokkeista lähellään (sama sääntö kuin isännällä) ja Aegis-kilpi toimii kaverillakin (`BlockWithAegis()` uudelleenkäytetty `current`in kautta). Testattu Unity CLI:llä Play Modessa `eval`-kutsuilla palvelin-omisteisella synteettisellä `RemotePlayerNet`illä (toista konetta ei käytettävissä): hahmovalinta-RPC, asevalinta-RPC, Sword tappoi oikean vihollisen ja kaveri sai XP:n/kolikon - konsoli puhdas (vain jo dokumentoidut Netcode-tuhoamis-NRE:t). **Tunnettu rajoitus löytyi testissä**: koska `TickNetworkCoop` kutsutaan isännän oman `Update()`in sisältä pelin päähaarasta, isännän kuolema (`Finished=true`) pysäyttää myös kaverin simulaation kokonaan, vaikka kaveri olisi yhä elossa - ei korjattu tässä, vaatii jatkokäsittelyä. Ei mukana (kuten aiemmin dokumentoitu): kaverin Silver-metaprogressio/kulta-Kauppa, uuden kierroksen/aluevaihdon täysi kaverin tilan resetointi, kahden oikean koneen testi. Suunnitelma: `C:\Users\marku\.claude\plans\squishy-coalescing-flame.md`.

- 2026-09-08: Korjattu käyttäjän testissä löytämä bugi: viholliset eivät koskaan kohdistaneet liikettään kaveripelaajaan (`RemotePlayerNet`), vain isäntään - kaveri saattoi seistä keskellä vihollislaumaa ilman että yksikään tuli häntä kohti, ellei sattumalta ollut jo isännän vieressä. Syy: `Update()`:n vihollissilmukka käytti kovakoodattua `player.position`-kohdetta sekä liikkeen suuntaan (`MapLayout.PursuitDirection`/`MoveOnMap`) että kääntymiseen. Uusi `NearestCombatantPosition(from)`-apumetodi (`SurvivorGameNetwork.cs`) valitsee liikkeen kohteeksi lähimmän elossa olevan pelaajan (isäntä tai mikä tahansa `RemotePlayerNet.Active`-listan kaveri); kontaktivahinko isäntään lasketaan yhä erikseen tuoreesta `player.position`-etäisyydestä ettei kaveri-kohdistus vahingossa muuta isännän omaa osumalogiikkaa. Kaverin oma kontaktivahinko/lähitaistelu hoidettiin jo ennestään `TickNetworkCoop`:ssa etäisyyspohjaisesti riippumatta siitä mitä vihollinen jahtaa. Varmennettu Unity CLI:n Play Modessa: isännöitiin oikeasti Relayn kautta, luotiin palvelin-omisteinen `RemotePlayerNet`-instanssi `eval`-kutsulla (koska toista konetta ei ole käytettävissä), ja kutsuttiin `NearestCombatantPosition`-metodia suoraan reflektiolla kahdesta eri pisteestä - piste lähempänä kaveria palautti kaverin sijainnin, piste lähempänä isäntää isännän sijainnin. Sivuhavaintoja testistä (ei koodimuutoksia): kaveri-instanssi kuoli oikeasti lähellä oleviin vihollisiin kontaktivahingosta testin aikana, mikä epäsuorasti vahvisti että `TickNetworkCoop`in kaverille kohdistuva vahinko toimii. Hahmonvalinta ei ole eikä ole ollut käytettävissä kaverille (`RemotePlayerNet` on aina kiinteä vihreä kapseli, ei asekaappia/leveleitä) - tämä on tarkoituksellinen vaiheen 1 rajaus, ei bugi, dokumentoitu jo aiemmassa moninpeli-merkinnässä. Ei buildia/julkaisua. Kahden oikean koneen testi on yhä tekemättä.

- 2026-09-08: Proseduraalinen huone-/käytävägeneraattori v1 oletuskartaksi (`ProceduralMapLayout.cs`, `SurvivorProceduralMaps.cs`). Jokaisessa kartassa 8–12 yhdistettyä huonetta, lisäsilmukoita, kaukainen bossihuone, arkut ja pyhäkkö; minimappi ja seedin syöttö/kopiointi. Bossin kuolema vaihtaa automaattisesti noin 2 s kuluttua uuteen seediin, siivoaa vanhan kartan resurssit/taisteluoliot ja siirtää pelaajan alkuhuoneeseen; build ja odottavat tasopäivitykset säilyvät. Aiempi 90 s bossiportaalisääntö ja karttanumeron vaikeusskaalaus säilyvät. Seed-historia, karttanumero ja generaattoriversio tallentuvat PlayerPrefsiin; samalla seedillä voi toistaa myös myöhemmän kartan geometrian. BFS-reitinhaku ja ruudukkopohjainen liike estävät vihollisia ja pelaajaa oikaisemasta seinien läpi. Moninpelille seed-/karttanumeroviesti liittymisessä ja siirtymässä; ei kahden koneen testiä. Unity CLI: 207 erilaista/toistettavaa/yhtenäistä karttaa, 222 onnistunutta reittiä, seinätestit ja kolme peräkkäistä bossisiirtymää läpäisty. Myös normaali Update-ajastin vaihtoi kartan 1 → 2 kesken odottavan tasovalinnan ilman uusia konsolivirheitä. Yhdessä käsin tehdyssä tarkistuksessa asetettiin vahingossa PendingChoices ilman vaihtoehtolistaa; tästä tulleet GUI-virheet olivat virheellisen testitilan seuraus, normaali GrantExperience-polku testattiin uudelleen virheittä. Aiemmat Netcode-tuhoamisen NRE:t säilyvät. Ohje: `Assets/Survivor/README-ProceduralMaps.md`; testit: `PrototypeTools/VerifyProceduralLayout.cs` ja `VerifyProceduralProgression.cs`. Ei julkaisua.

- 2026-09-08: Uusi Mosswood-metsäkartta nykyisen pyöreän areenan tilalle (`SurvivorForestMap.cs`). Tasainen, epäsymmetrinen 10-sivuinen alue, noin 195 × 159 yksikköä nykyisellä arenaRadius=60-asetuksella ja yli 1,8-kertainen pinta-ala. 88 puuta/lohkaretta, sammalläikkiä, kivinen reunus ja reunapuita; polut nykyisille arkuille, pyhäkölle ja portaalille. Kiinteä oma satunnaissiemen säilyttää saman geometrian hostilla ja asiakkaalla muuttamatta pelin Random-tilaa. Pelaaja, väistö, viholliset ja RemotePlayerNet käyttävät yhteisiä karttarajoja ja maastoesteiden ohitusta; vihollis- ja bossispawnit sijoitetaan vapaalle maalle. Unity CLI Play Mode -tarkistus (`PrototypeTools/VerifyForestMap.cs`): 2000 rajaprojektiota, kaikkien 88 esteen ohitus, 120 kulmaspawnia, kaikkien maamerkkien saavutettavuus ja bossin kutsuminen läpäisty. Pelinäkymä ja yleiskuva tarkistettu (`PrototypeTools/forest-game.png`, `forest-overview.png`). Konsolissa vain aiemmin dokumentoidut Netcode-tuhoamisen NullReferenceExceptionit, ei kartan käännös-/ajovirheitä. Kahden koneen moninpeliä ei testattu. Ei buildia tai julkaisua.

Lyhyt loki tehdyistä muutoksista. Uusin ylimpänä.

- 2026-09-08: Julkaistu v0.3.0 (edellinen: v0.2.2). Buildattu Unity CLI:llä (StandaloneWindows64, 0 virhettä, 6 varoitusta), zipattu ja liitetty GitHub-releaseen [v0.3.0](https://github.com/Marcebooy/SurvivorPrototype/releases/tag/v0.3.0). Kaikki edellisen releasen jälkeiset committamattomat muutokset (4 uutta hahmoa, moninpelin vaihe 1, kaikkien 30 aseen osumaefektit, ase-/Tome-slottikatot, Netcode-siivous) commitoitu ja pushattu ensin (commit `b935109`). Discord-ilmoitus lähetetty webhookilla käyttäjän vahvistuksen jälkeen.
- 2026-09-08: Iskuosumille lisätty asset-pakettien VFX-efektejä, joita projektissa ei vielä käytetty (aiemmin vain Lightning ja Aura käyttivät oikeita partikkeliefektejä, loput 28 asetta pelkkiä primitiivi-"Shape"-muotoja). Kuusi uutta `SurvivorGame`-kenttää (`slashEffect`, `fireImpactEffect`, `poisonImpactEffect`, `frostImpactEffect`, `voidImpactEffect`, `boltImpactEffect`) kytketty Unity CLI:llä Survivor Game -olioon: `FX_Orange_Slash_1` (Eric VFX Studio) terä-/lähitaisteluaseille (Sword, HeroSword, CorruptedSword, Scythe, Katana, Dexecutioner, Axe), `FX_Fireball` (Eric VFX Studio) räjähtäville/tuliaseille (Rocket, DragonBreath, Mines), `FX_Green_Hit` (Eric VFX Studio) Poison Flaskille, ja kolme uutta väriä Zap VFX URP -paketista jo käytössä olleen sinisen (Lightning) rinnalle: valkoinen Frostwalkerille, musta Black Holelle ja Space Noodlelle, violetti lopuille etä-/taika-aseille (Bow, Sniper, Revolver, Bananarang, Wireless Dagger, Aegis, Dice, Blood Magic, Tornado). Uusi `ImpactEffectFor`/`SpawnImpact`-apupari (`SurvivorAdvancedWeapons.cs`) kytketty jaettuihin `AdvancedPulse`/`AdvancedBeam`-funktioihin (kattaa suoraan 12 asetta kerralla), projektiiliosumiin (`TickAdvancedShots`), vyöhykkeiden syntyhetkeen (`AddAdvancedZone` - kertaluontoinen "cast"-purske, ei toistuvaa spämmiä tick-silmukassa) sekä erikseen Swordiin (`AttackWeapons`) ja Bow'n nuoliosumiin (`TickBolts` - Bow puuttui aiemmin osumaefektistä kokonaan). Koko skaalattu `Size`-statin mukana kuten Aura jo aiemmin. Play Modessa visuaalisesti todennettu (kaikki 30 asetta tasolle 2, viholliset ympärillä): `FX_Fireball` osoittautui autorisoitu ~9x liian suureksi tämän pelin mittakaavaan (natiivilla skaala 1:llä yksittäinen räjähdys peitti koko areenan keskustan, moninkertaisesti hahmoa suurempi) - kaikki muut paketit (viilto, myrkky, jää, tyhjiö, taikaosuma) näyttivät oikean kokoisilta jo skaalalla 1. Lisätty `ImpactBaseScale`-kerroin (0.15) vain tulipaketille korjaamaan tämä; muut käyttävät suoraan `Size`-statin mukaista skaalaa ilman erillistä korjausta, samaan tapaan kuin Lightningin Zap-efekti jo aiemmin. Ei uusia konsolivirheitä (ainoat esiintyneet olivat jo dokumentoidut Netcode `NetworkObject.OnDestroy`-NRE:t, toistuivat koska Play Mode käynnistettiin/pysäytettiin testissä useaan kertaan).
- 2026-09-08: Korjattu suurin osa Play Modea pysäytettäessä syntyneistä Netcode-konsolivirheistä (41 → 2). Syy: Unity tuhoaa scenen oliot Stopissa ilman taattua järjestystä, ja jos `NetworkManager` tuhoutuu ennen verkotettuja `NetworkObject`-olioita (viholliset, RemotePlayerNet), nämä yrittävät sen jälkeen viitata jo nollattuun `NetworkManager`-tilaan → `RemoveNetworkObjectFromSceneChangedUpdates`-NRE-vyöry. Lisätty `SurvivorNetwork.cs`iin `OnApplicationQuit`- ja Editor-kohtainen `EditorApplication.playModeStateChanged`-kytkentä (`ExitingPlayMode`), jotka kutsuvat `Disconnect()`/`NetworkManager.Shutdown()`ia siististi *ennen* kuin Unity ehtii aloittaa olioiden tuhoamisen. Testattu Unity CLI:llä Play Mode -käynnistyksellä/pysäytyksellä: 41 virhettä → 2. Jäljellä olevat kaksi `NullReferenceException`-riviä (`NetworkSceneManager.Dispose()`, `SceneEventDataStore` on `null` jos verkkopeliä ei koskaan käynnistetty) ovat Netcode for GameObjects -paketin oma bugi (puuttuva null-tarkistus `Library/PackageCache`-koodissa, ei korjattavissa projektista) - nämä olivat jo aiemminkin tiedossa (ks. alla oleva 2026-09-08-merkintä "Neljä uutta pelattavaa hahmoa"), käyttäjä päätti jättää ne ennalleen koska ne ovat harmittomia eivätkä riko peliä.
- 2026-09-09: Kaksoiskajo-assettipaketti (Sword + Kaksoisterä, molemmat FBX + tekstuurit, `Unity/Kaksoiskajo/`) tuotu projektiin ja kytketty puhtaasti visuaalisesti Sword-aseen/Kaksoisterä-variantin näkyvyyteen - ei kosketa vahinkolaskentaan, statseihin tai muuhun pelimekaniikkaan (kaikki jo `WeaponVariants.cs`:ssä). **Tutkimus ennen koodausta**: projektissa ei ollut mitään yleistä ase-prefab/mesh-viittausjärjestelmää - jokainen ase renderöityi pelkkinä primitiiveinä (`Shape()`-kutsut), ja Weapon.Sword näytti eri asian eri hahmoilla (Pottu: proseduraalinen paistinpannu `PottuVisual.cs`:ssä; Ritari: miekka valettu kiinteästi hänen riggattuun FBX-meshiinsä, ei erillinen objekti). Kumpikaan ei tarjonnut valmista "vaihda ase-mesh"-koukkua, ja Mappi-tilan omistuspohjainen loadout sallii minkä tahansa hahmon omistaa Swordin, joten päädyttiin hahmoriippumattomaan ratkaisuun. Tuonti: `Assets/Survivor/Weapons/Kaksoiskajo/Resources/` (kaksi FBX:ää + `Textures/`-kansio, 15 PNG:tä; `*_MetallicSmoothness.png`-kartat tuotu sRGB pois päältä). Uusi `KaksoiskajoImportPostprocessor.cs`: Scale Factor 1, ei riggausta/animaatiota, remappaa viisi materiaalia (SilverSteel/AntiqueGold/NavyLeather/TurquoiseCrystal/Lightning) URP/Litiin `_BaseMap`/`_MetallicGlossMap`(`_SmoothnessTextureChannel=0`, Metallic Alpha -lähde)/`_EmissionMap`-kartoilla; HDR-emissiokerroin lähdepaketin READMEn ohjearvoista (kristalli 2.2, salama 5, muut materiaalit eivät hehku). **Tekninen sudenkuoppa**: `OnPostprocessMaterial`-koukku ei koskaan laukea tälle FBX:lle (varmennettu debug-merkein - `OnPreprocessModel` samalla luokalla laukeaa luotettavasti, mutta materiaalikohtainen koukku ei koskaan, syytä ei selvitetty tarkemmin) - vaihdettu `OnPostprocessModel(GameObject root)`:iin, joka kävelee tuodun hierarkian rendererit läpi suoraan ja toimii luotettavasti. Uusi hahmoriippumaton visuaalinen koukku `SwordVariantVisual.cs`: instantioi molemmat mallit kertaalleen pelaajan `player`-transformin lapsiksi (ei kosketa `PottuVisual`/`RitariVisual`/`ICharacterVisual`iin), `TickSwordVariantVisual()` kutsuttuna `Update()`:sta `TickArsenal`in vierestä - Sword-malli aktiivinen kun `weaponLevels.ContainsKey(Weapon.Sword)` ja `ActiveVariant(Weapon.Sword) != 1`, Kaksoisterä-malli kun sama mutta `== 1`, molemmat pois kun Swordia ei omisteta. **Tunnettu rajoitus, kirjattu koodikommenttiin**: kummallakaan hahmolla ei ole oikeaa kädensocketia - mallit kiinnitetty suoraan pelaajan runkoon kiinteällä approksimoidulla paikallissijainnilla/rotaatiolla (`SwordAttachLocalPosition/Rotation`); korvattava oikealla socket-transformilla kun/jos hahmoille lisätään ase-attachment-piste. Testattu Unity CLI:llä Play Modessa reflektiolla: perusase aktivoi Sword-mallin (Kaksoisterä pois), `SetActiveVariant`-kutsu vaihtoi mallin Kaksoisteräksi ja takaisin oikein molempiin suuntiin, Swordin poisto piilotti molemmat mallit vaikka variantti oli päällä, ja Bow'n varustaminen ei koskenut Sword-malleihin mitenkään (regressiotesti). Kuvakaappauksilla (kameran tilapäinen siirto `Time.timeScale=0`:lla) vahvistettu molempien mallien renderöityvän oikein tekstuureineen - Swordin teräs+turkoosi kristalli näkyi, Kaksoisterän kaksi terää + valkoinen salamayhteys niiden välissä näkyi. Ei uusia konsolivirheitä (tuttu Netcode-poikkeus play modesta poistuttaessa, ei liity tähän). Ei committia vielä tälle osalle.
- 2026-09-09: HAHMOT-tabi laajennettu keskitetyksi hahmoprofiiliksi: karttavarastoyhteenveto + muokattava oletusloadout (`CharacterShop.cs`). Uusi tiivis `MapInventorySummary()` (`MapInventory.cs`) listaa vain nollasta poikkeavat (tyyppi,Tier)-parit ("Mosswood T1×3, Luuluola T2×1") sen sijaan että toistaisi koko MAPIT-tabin taulukkoa. Uusi pysyvä `Dictionary<PlayerCharacter, HashSet<int>> defaultLoadout` (`CharacterShop.cs`) - täsmälleen sama id-muoto kuin `mapLoadout`illa (aseet `WeaponUpgradeId`:n kautta, Tomet suoraan), tallennus `PlayerPrefs`iin per hahmo (`DefaultLoadout_"+character`, pilkuin eroteltu id-lista) samalla mallilla kuin `ownedUpgrades`/`OwnedKey`. Variantin toggle/laatutaso ei tarvinnut uutta rakennetta - `activeVariantByCharacter`/`SetActiveVariant`/`VariantQuality`/`UpgradeVariantQuality` olivat jo per-hahmo ja pysyviä, HAHMOT-tabi vain näyttää saman UI:n `shopCharacter`illa `SelectedCharacter`in sijaan. Uusi `DrawDefaultLoadoutSection()` toistaa `DrawLoadoutSelect`in grid-tyylin (omistetut aseet/Tomet, variantti-tähdet, Paranna-nappi) mutta kirjoittaa `ToggleDefaultLoadout()`:in kautta pysyvään `defaultLoadout`iin `loadoutWeapons`/`loadoutTomes`-staging-setin sijaan. **Jako Mappi-runin loadout-ruudun kanssa**: `ResetProgression()` esitäyttää `loadoutWeapons`/`loadoutTomes` suoraan `DefaultLoadoutFor(SelectedCharacter)`:sta kun `awaitingLoadoutChoice` on tosi (ohittaen ei-omistetut id:t `IsUpgradeOwned`-tarkistuksella, jos omistus on muuttunut tallennuksen jälkeen) - esitäyttö on vain lähtökohta, ei lukko: `ConfirmLoadout()`/`SkipLoadout()` eivät koskaan kirjoita takaisin `defaultLoadout`iin, joten run-kohtainen muokkaus alkuruudussa ei muuta pysyvää oletusta. HAHMOT-tabin koko sisältö kääritty `GUI.BeginScrollView`iin (uusi `characterTabScroll`-kenttä) koska uudet osiot eivät enää mahtuneet 720px-korkealle ruudulle - kaikki olemassa olevat kaupan absoluuttiset koordinaatit pysyivät muuttumattomina (viewport on koko näytön kokoinen, joten vanha sisältö renderöityy identtisesti, uudet osiot vain jatkuvat scrollattavana alaspäin). CharacterShopin ostologiikkaa (`BuyCharacterUpgrade`/`UpgradePrice`/`DrawShopSlot`) ei koskettu. Testattu Unity CLI:n `eval`-kutsuilla Play Modessa reflektiolla (yksi kesken jäänyt Play-sessio jouduttiin käynnistämään uudelleen puhtaana - pitkän istunnon `current`-kenttä oli jäänyt nulliksi edellisen domain-reloadin jäljiltä, ei liity tähän muutokseen): Selviytymistila varmistettu täysin koskemattomaksi - `defaultLoadout` asetettuna ja omistettuna, `activeMapType=Procedural` silti antoi `awaitingLoadoutChoice=False` ja tyhjän `loadoutWeapons`in. Mosswoodilla esitäyttö toimi oikein (`loadoutWeapons=[0,1]` täsmälleen tallennetun oletuksen mukaan). Run-kohtainen muokkaus vahvistettu ei-kirjoittavaksi: Bow poistettiin run-poolista ja `ConfirmLoadout()` kutsuttiin - `mapLoadout` päivittyi (`[0]`), mutta `defaultLoadout`/`PlayerPrefs` pysyivät muuttumattomina (`[0,1]`). Omistuksen muuttumisen turvaverkko testattu erikseen: oletusloadoutiin lisätty omistamaton ase (Chunkers) - esitäyttö ohitti sen oikein eikä tuonut sitä run-pooliin. Ei uusia konsolivirheitä feature-koodista (kaikki nähdyt olivat joko tuttuja Netcode-poikkeuksia tai edellä mainitun `current`-nollautumisen jäljiltä ennen puhdasta uudelleenkäynnistystä). Testidatan PlayerPrefs-avaimet ja `mapItems`-testimäärät siivottu takaisin tarkalleen alkuperäisiin arvoihinsa lopuksi. **Committattu** (commit `182b547`) samalla väliaikainen-palautus-menettelyllä kuin aiemmissakin: `SurvivorProgression.cs` sisälsi jälleen samaan aikaan toisen istunnon VFX-poolaus/Inferno-bossityötä (`AttachWeaponVfx`, `CreateNecromancerBoss`, `boss.animator`) ja AGENTS.md sisälsi lisäksi kolmannen, uuden "Tuhkaerämaa"-ympäristöpaketin oman Muutosloki-kappaleensa - molemmat palautettiin käsin HEAD:in muotoon ennen commitointia ja takaisin heti perään (`SurvivorAdvancedWeapons.cs` + paketti-/projektiasetus-/Inferno-/AshWastes-/VFX-tiedostot `git stash`illa), diff täsmäsi tarkalleen. Committiin päätyi vain `CharacterShop.cs`:n/`MapInventory.cs`:n uudet rivit, `SurvivorGame.cs`:n `LoadDefaultLoadout()`-kutsu ja `SurvivorProgression.cs`:n esitäyttölisäys.
- 2026-09-09: Variantin laatutaso (1-3 tähteä) toteutettu Sword/Bow/Chunkers-varianteille (`WeaponVariants.cs`, ks. alla oleva "Ase-variantit"-merkintä perusjärjestelmästä). Uusi globaali (ei per hahmo, kuten `unlockedVariants`) `Dictionary<(Weapon,int),int> variantQuality` + `int qualityMaterial` -pino-valuutta, molemmat `PlayerPrefs`issa (`VariantQuality_"+w+"_"+v` / `QualityMaterial`). Laatu alkaa tähdestä 1 heti avauksesta, ei erillistä nollatilaa. **Kolmas, täysin oma bossinkaatorulla** `TryDropQualityMaterial()` (15 %, riippumaton 18 %:n `TryDropMapItem`ista ja 10 %:n `TryDropWeaponVariant`ista) - droppaa materiaalia vain jos jollain omistetulla ja jo avatulla variantilla on vielä tilaa parantua (laatu < 3), muuten ei anna hyödytöntä valuuttaa; sama Mappi-tila-gate kuin muillakin (`activeMapType != Procedural`). `UpgradeVariantQuality()` EI avaa uutta varianttia, vain parantaa jo avattua - vaatii omistuksen (`IsUpgradeOwned`) ja avauksen (`IsVariantUnlocked`) ennen kulutusta; kasvava hinta 1 kpl (1→2) / 2 kpl (2→3), kuten `UpgradePrice`. Bonus maltillinen: `VariantQualityMultiplier = 1 + .1*(laatu-1)` (tähti 1=1.0x, 2=1.1x, 3=1.2x, max +20 %) kerrottuna kunkin variantin ydinstattiin - Kaksoisterän kokonaisvahinkoon (`fullPower`, `AttackWeapons`), Kimmokaaren ammuksen vahinkoon (bolt `power`) ja Iskukivien aluevahinkoon (`AreaHit`-kutsu `TickOrbits`issa). Loadout-ruutu (`DrawLoadoutSelect`) näyttää tähdet (★★☆) suoraan variantti-togglenapin tekstissä ja pienen "Paranna (X kpl)" -napin sen alla kun laatu < 3, sekä materiaalin kokonaismäärän ASEET-otsikkorivillä. Testattu Unity CLI:n `eval`-kutsuilla Play Modessa reflektiolla: gate varmennettu kahdesti - 300 yritystä ilman yhtään omistettua/avattua kelvollista kohdetta (Procedural) antoi 0 droppia, ja 300 yritystä Sword omistettuna+avattuna mutta Procedural-tilassa antoi myös 0 droppia (Selviytymistila täysin koskematon vaikka kelvollinen kohde olisi olemassa) - vasta Mosswoodilla droppi alkoi toimia. Droppijakauma varmennettu 4000 kutsun otoksella: 616/4000 = 15,4 % (tavoite 15 %). Hintaporrastus varmennettu askel askeleelta: 0 materiaalilla 1→2-yritys epäonnistui, 1 materiaalilla onnistui ja kulutti tasan sen, välitön 2→3-yritys 0 materiaalilla epäonnistui (hinta nyt 2), 2 materiaalilla onnistui ja kulutti tasan sen, yritys laadun ollessa jo 3 epäonnistui vaikka materiaalia oli 5, ja Bow-variantin parannusyritys epäonnistui koska sitä ei ollut vielä avattu. Taistelukytkentä varmennettu suoralla laskennalla: damage=18, Quantity=1, laatu=3 (mult=1.2) → fullPower=18×1.2=21.6, mainPower=21.6×.65=14.04 - täsmäsi `Hit()`:in mittaamaan vahinkoon pilkulleen. Ei uusia konsolivirheitä (kaikki nähdyt olivat olemassa olevia Netcode-poikkeuksia tai edellisen istunnon jälkijättöisiä `git stash pop`/käännösvirheitä ennen tätä muutosta, ei liity tähän). Testidatan PlayerPrefs-avaimet siivottu pois lopuksi. **Committattu** (commit `b3e23ff`) samalla väliaikainen-palautus-menettelyllä kuin edellinenkin variantti-committi: `SurvivorArsenal.cs`/`SurvivorProgression.cs` sisälsivät jälleen samaan aikaan toisen istunnon VFX-poolaus/Inferno-bossityötä (`AttachWeaponVfx`, `CreateNecromancerBoss`, `boss.animator` ym.) samoissa tiedostoissa - palautettiin käsin HEAD:in muotoon ennen commitointia ja takaisin heti perään (`SurvivorAdvancedWeapons.cs` + paketti-/projektiasetustiedostot `git stash`illa), diff täsmäsi tarkalleen. Committiin päätyi vain `WeaponVariants.cs`:n uudet laatutasorivit, `SurvivorGame.cs`:n `LoadVariantQuality`/`TryDropQualityMaterial`-kutsut ja `SurvivorArsenal.cs`/`SurvivorProgression.cs`:n `VariantQualityMultiplier`-kertoimet + loadout-ruudun tähti-/parannus-UI.
- 2026-09-09: Ase-variantit toteutettu pilottina kolmelle aseelle (Sword, Bow, Chunkers) - uusi `WeaponVariants.cs`. Sama ase, vaihtoehtoinen käytös, ei vie omaa loadout-paikkaa: valinta tallennetaan per hahmo (`activeVariantByCharacter`, `SetActiveVariant`/`ActiveVariantFor`), erillään loadoutWeapons/mapLoadoutista - valinta säilyy vaikka ase poistettaisiin loadoutista, mutta vaikuttaa taisteluun vasta kun weaponLevels sisältää aseen jälleen (ei vaatinut erillistä sulkulogiikkaa, koska kaikki `ActiveVariant`-luvut taisteluikoodissa ovat jo `weaponLevels.TryGetValue`-tarkistuksen sisällä). Avataan pysyvästi pelaamalla, ei Silverillä: `TryDropWeaponVariant()` (oma 10 % rulla, riippumaton `TryDropMapItem`ista) kutsutaan `Hit()`:in bossinkaatohaarasta, mutta vain kun `activeMapType != Procedural` (Selviytymistila ei koskaan kutsu tätä, todennettu 200 epäonnistuneella yrityksellä Play Modessa) ja vain aseille jotka pelaaja jo omistaa CharacterShopin/HAHMOT-tabin kautta (`IsUpgradeOwned`-tarkistus). Käytösmuunnokset (ei vain numerot): **Sword → Kaksoisterä** - yksi vahvempi isku korvataan kahdella pienemmällä, joista toinen laukeaa 0.12 s viiveellä (`swordEchoTimer`/`SwordEchoStrike`, `SurvivorGame.cs`/`AttackWeapons`). **Bow → Kimmokaari** - Quantity-määrän suoria läpäiseviä nuolia korvaa yksi vahvempi nuoli joka kimpoaa lähimpään uuteen viholliseen 1+Quantity kertaa (`Bolt.chain/chainLeft`, `UpdateBolts`). **Chunkers → Iskukivet** - jatkuvan kosketusvahingon sijaan kivet iskevät ajoittain räjähtävän aluevahingon lähimpään viholliseen, painottaen Cooldownia/Sizea Quantityn sijaan (`chunkersBlastTimer`, `TickOrbits`, `SurvivorArsenal.cs`). Mappi-tilan loadout-ruutuun (`DrawLoadoutSelect`) lisätty pieni togglenappi aseen alle kun variantti on avattu. Testattu Unity CLI:n `eval`-kutsuilla Play Modessa reflektiolla (ei visuaalista kuvakaappausta - OnGUI-pohjainen käyttöliittymä ei tallentunut `capture_game_view`/`screenshot`-työkalujen kautta, ilmeinen työkalurajoitus immediate-mode GUI:lle): Kaksoisterä teki kaksi erillistä osumaa, Kimmokaari loi tasan yhden ketju-ammuksen (chainLeft=2), Iskukivet laukaisi aluevahingon ja ladasi ajastimen uudelleen, ja Selviytymistila (`activeMapType=Procedural`) esti droppauksen täysin 200 yrityksen yli. Testidatan PlayerPrefs-avaimet siivottu pois lopuksi. Ei uusia konsolivirheitä (kaikki nähdyt olivat olemassa olevia Netcode-poikkeuksia). **Committattu yhdessä HAHMOT-tabin/CharacterShopin ja Mappi-tilan omistuspohjaisen loadoutin kanssa** (ks. yllä olevat kaksi merkintää) - variantit riippuvat niistä suoraan eivätkä käänny/toimi ilman niitä. Samalla `SurvivorArsenal.cs`, `SurvivorProgression.cs`, `Packages/manifest.json`, `Packages/packages-lock.json`, `ProjectSettings/ProjectSettings.asset` ja `SurvivorAdvancedWeapons.cs` sisälsivät tähän liittymätöntä, toisen istunnon kesken olevaa työtä (uusi VFX-poolausjärjestelmä `AttachWeaponVfx`/`WeaponZoneMaterial`/`WeaponVfxPool.cs` ym., Inferno-bossiareena `CreateNecromancerBoss`/`boss.animator`, post-processing-paketti) - nämä rivit palautettiin väliaikaisesti HEAD:in muotoon ennen commitointia (`SurvivorAdvancedWeapons.cs` + paketti-/projektiasetustiedostot kokonaan `git stash`illa, `SurvivorArsenal.cs`/`SurvivorProgression.cs` käsin rivikohtaisesti) ja palautettiin takaisin nykyiseen muotoonsa heti commitin jälkeen (`git stash pop` + varmuuskopioiden palautus) - sama menettely kuin aiemmissakin MAPIT/Mappi-inventaario-committeissa.
- 2026-09-08: Neljä uutta pelattavaa hahmoa lisätty: Golem, Metsästäjä, Ninja, Paladiini (yhteensä nyt 9 hahmoa). Jokainen samalla FBX-tuontikaavalla kuin Ritari/Berserkeri (`Assets/Survivor/Editor/<Nimi>ImportPostprocessor.cs`, Legacy-rig, URP-materiaalit Blenderin väriarvoista, malli+animaatiot kopioitu `Unity/<Nimi>/` → `Assets/Survivor/Characters/<Nimi>/Resources/`). Aloitusase-valinnat ja perustelut: **Golem** → Chunkers (malliin ei kuulu lähitaisteluasetta erikseen, ja mallin "kiertävät kivet" -shokkiaalto-VFX muistuttaa suoraan Chunkersia); uusi `ICharacterVisual.Shockwave()`-koukku laukeaa jokaisesta osumasta (`ReceiveDamage`), toistaen Stoneform_Shockwave-animaation - readmen oma ehdotus ja täsmää AGENTS.md:n passiiviin. **Metsästäjä** → Bow (readmessä vain yksi vaihtoehto); Bow-aseelle ei ollut aiemmin hahmokoukkua, lisätty `characterVisual.Swing(aim)` `AttackWeapons()`:n Bow-haaraan. **Ninja** → Katana eikä Wireless Dagger (Katanalle lisätty vastaava `Swing()`-kutsu `CastAdvanced`:n Katana/Dexecutioner/BloodMagic/Dice-ryhmään); Shadowstep-animaatio korvaa muiden hahmojen geneerisen "rullaa"-väistöasennon lukemalla jaettua `roll`-parametria `Tick()`:ssä, ei vaatinut uutta rajapintakoukkua. **Paladiini** → Aura eikä Aegis (sama päättely kuin Ritarin Sword > Aegis -valinnassa: Aegis yksinään ei tuota jatkuvaa vahinkoa), mutta Attack_CleanSlash/Block_Holy kytketty siltä varalta että Sword-perhe tai Aegis poimitaan myöhemmin; Divine_Beam jää käyttämättä (ei vastaavaa asetta). Hahmovalintaruutu muutettu yksiriviseltä ruudukoksi (5 saraketta, 2 riviä 9 hahmolle, `DrawCharacterSelect`), ja korttien kuvausteksti sai oman pienemmän fonttityylin (`cardTextStyle`) ja "Valitse"-napin korkeutta kasvatettiin ettei teksti leikkaudu. Testattu Unity CLI:llä Play Modessa: kaikki neljä valittavissa, aseet tekevät vahinkoa (Aura-kehä näkyvissä heti, Chunkers-kivet kiertävät), Ninjan Shadowstep ja Golemin Shockwave laukesivat virheittä suoralla reflektiokutsulla. Ainoat uudet konsolivirheet ovat Unity Netcode -paketin omia (NetworkManager/NetworkObject-tuhoamisen null-viittaukset, dokumentoitu jo yllä olevassa merkinnässä) - ei liity hahmoihin.
- 2026-09-08: Asepäivitysten saanti rajattu: pelaaja voi run-kohtaisesti omistaa enintään 5 eri asetta ja 5 eri Tomea (`MaxWeaponKinds`/`MaxTomeKinds`, `SurvivorProgression.cs`). Sekä tasopäivitysvalinnat (`RollChoices`) että arkkujen palkinnot (`Interact`, kind 0 - käytti aiemmin täysin suodattamatonta `Random.Range(0, UpgradeNames.Length)`ia, joten arkku saattoi jo aiemmin rikkoa Quantity/Crit-katot; nyt molemmat käyttävät samaa `BuildUpgradePool()`ia) noudattavat samaa rajaa: jo omistettuja aseita/Tomeja voi yhä tasottaa loputtomiin, mutta 6. eri lajia ei enää tarjota kun kumpikin katto on täynnä. Tomeille lisätty oma seuranta (`tomeLevels`, uusi `TomeCount`-ominaisuus `WeaponCount`-parin rinnalle) - aiemmin Tomet olivat pelkkiä suoraan sovellettavia statbonuksia ilman omistustietoa. HUD näyttää nyt "Aseet X/5 • Tomet X/5" (`DrawProgression`). Motivaatio: usealla hahmolla on nyt oma aloitusase (Ritari/Velho/Necromancer/Berserkeri), ja rajattu slottimäärä pakottaa build-valintoja sen sijaan että 90 sekunnin sisään saisi teoriassa kaikki 30 asetta. Testattu Unity CLI:n `eval`-kutsulla Play Modessa: reflektiolla ajettu 300 satunnaista `ApplyUpgrade`-kutsua `BuildUpgradePool()`in kautta Ritarilla - aseita ja Tomeja jäi tasan 5/5, kumpikaan ei ylittänyt kattoa. Ei uusia konsolivirheitä (kaksi olemassa olevaa Netcode `NetworkManager.OnDestroy`-poikkeusta play modesta poistuttaessa, ei liity tähän muutokseen - esiintyi jo ennen tätä muutosta moninpelipaketin lisäyksen myötä).

- 2026-09-08: Moninpelin vaihe 1 (kaverin kanssa pelaaminen Relayn kautta) toteutettu. Lisätty paketit `com.unity.netcode.gameobjects` (2.13.2), `com.unity.services.relay` (1.2.0), `com.unity.services.authentication` (3.7.4), `com.unity.services.core` (1.18.0) - projekti oli jo linkitetty Unity Cloud -projektiin (`cloudProjectId`), joten Relay toimi suoraan ilman dashboard-asetuksia. Uudet skriptit: `SurvivorNetwork.cs` (UGS-alustus, anonyymi kirjautuminen, Relay-allokaatio/liittymiskoodi, host/join), `RemotePlayerNet.cs` (kaverin hahmo - yksinkertainen vihreä kapseli, kiinteä lähitaistelu, ei omaa asekaappia/leveleitä vielä), `ClientAuthoritativeTransform.cs` (kaverin oma kone ohjaa liikettä, ei isäntäviivettä), `SurvivorGameNetwork.cs` (host-only kontaktivahinko/lähitaistelutikki kaverille, asiakaskoneen kamera/HUD). Isäntä pelaa täysin ennallaan (`SurvivorGame.Update`/`SpawnEnemy` gatetaan `NetworkManager.Singleton`-tilalla); liittyvä kaveri ei aja omaa simulaatiota, vaan renderöi isännän deterministisen areenan (`BuildWorld` ei käytä satunnaisuutta) ja isännän replikoimat viholliset (`NetworkObject`/`NetworkTransform` lisätty `UD_demo_character.prefab`iin). Päävalikkoon uusi "MONINPELI"-paneeli (`SurvivorGame.cs: DrawMultiplayerPanel`): "ISÄNNÖI KAVERILLE" / liittymiskoodikenttä / "LIITY KOODILLA". Testattu Unity CLI:llä Play Modessa `eval`-kutsuilla: `HostGame()` tuottaa oikean Relay-liittymiskoodin ja `NetworkManager.StartHost()` onnistuu virheittä (huomio: `AddNetworkPrefab`-kutsut piti poistaa, koska NGO rekisteröi molemmat verkko-objektit jo automaattisesti `Assets/DefaultNetworkPrefabs.asset`iin - kaksinkertainen rekisteröinti aiheutti "duplicate GlobalObjectIdHash"-virheitä). Toista pelaajaa (LIITY KOODILLA -polkua, kaverin näkökulmaa) ei ole vielä testattu oikeasti kahdella koneella - vaatii käyttäjän oman testin kaverin kanssa internetin yli. Ei mukana: kaverin oma ase-/level-/kauppajärjestelmä, hahmovalinta kaverille, täysi ammusten replikointi, yli 2 pelaajaa, launcher/release-integraatio. Suunnitelma: `C:\Users\marku\.claude\plans\squishy-coalescing-flame.md`.

- 2026-09-08: Berserkeri lisätty viidenneksi pelattavaksi hahmoksi (`BerserkerVisual.cs`, `Assets/Survivor/Editor/BerserkerImportPostprocessor.cs`, kolme FBX:ää `Unity/Berserker/` → `Assets/Survivor/Characters/Berserker/Resources/`, sama Ritari-tyylinen kolmen-FBX-yhden-rigin yhdistely). Aloitusaseeksi valittu Corrupted Sword eikä Axe, vaikka malli kantaa kahta kirvestä: Axe on erillinen heittoprojektiili joka ei laukaisisi hahmon omaa `Attack_Double`-animaatiota, kun taas Corrupted Sword käyttää samaa lähitaistelu-Swing()-koukkua kuin Ritarilla ja sen vahinkokaava (`1+2*(1-HP/maxHP)`) vastaa suoraan mallin sisäänrakennettua Rage-järjestelmää. Lisätty uusi HP-pohjainen animaatiotilan vaihto: kun terveys alle 30 %, kävely vaihtuu paikallaan-pysyvään Rage_LowHP-hengitys-/kipinäsilmukkaan (README:n ehdottama "rage = 1 - HP/maxHP" -kytkentä, toteutettu tilanvaihtona koska Legacy-animaatio ei tue jatkuvaa blendausta). Tätä varten `ICharacterVisual.Tick` sai uuden `healthFraction`-parametrin - päivitetty kaikkiin viiteen hahmovisuaaliin, vaikka vain Berserkeri käyttää sitä toistaiseksi. Testattu Unity CLI:llä Play Modessa: viiden hahmon valintaruutu, Corrupted Sword tekee jatkuvaa vahinkoa, Rage-asento laukeaa virheittä kun HP pakotettiin alle kynnyksen. Ei uusia konsolivirheitä.
- 2026-09-08: Necromancer lisätty neljänneksi pelattavaksi hahmoksi (`NecromancerVisual.cs`, `Assets/Survivor/Editor/NecromancerImportPostprocessor.cs`, malli kopioitu `Unity/Necromancer/Necromancer_Glide.fbx` → `Assets/Survivor/Characters/Necromancer/Resources/`). Sama rakenne kuin Velholla: yksi FBX, yksi looppaava Glide-animaatio, Legacy-rig, ei lähitaisteluanimaatiota. Aloitusaseeksi valittu Bone (Pomppuluu) eikä Scythe - malli kantaa pelkkää luusauvaa/kelluvaa kalloa ilman minkäänlaista lähitaisteluasetta, ja Bone (etähyökkäys, kimpoaa vihollisten välillä) sopii sekä visuaaliseen että AGENTS.md:n "kutsuihin ja tappojen ketjuttamiseen" -kuvaukseen paremmin. Hahmovalintaruutu muutettu 2 kortin kiinteästä layoutista dataan perustuvaksi `CharacterRoster`-taulukoksi (`SurvivorPottu.cs: DrawCharacterSelect, DrawCharacterCard`), joten uusien hahmojen lisääminen jatkossa on yksi taulukkorivi. HUD:n hahmonimi-otsikko sai oman pienemmän fonttityylin (`hudNameStyle`), koska "NECROMANCER / LUU" ei mahtunut aiempaan otsikkokokoon. Testattu Unity CLI:llä Play Modessa neljän hahmon valintaruutu ja Necromancerin pelillinen toiminta; ei uusia konsolivirheitä.
- 2026-09-08: Ritari lisätty kolmanneksi pelattavaksi hahmoksi Potun ja Velhon rinnalle (`SurvivorPottu.cs: DrawCharacterSelect, SelectCharacter, BuildCharacterVisual` - hahmovalintaruutu laajennettu kolmeen korttiin). Aloitusaseeksi valittu Sword eikä Aegis, vaikka hahmon malliin kuuluu kiinteästi sekä miekka että kilpi: Aegis yksinään laukeaa vain pelaajan ottaessa osuman eikä tuota jatkuvaa vahinkoa, joten se sopii huonosti aloitusaseeksi tässä pelissä - Aegis jää luontevaksi myöhemmäksi poiminnaksi. Ritarin visuaali (`RitariVisual.cs`) on ainoa toistaiseksi jolla on oikeat hyökkäys- ja torjunta-animaatiot (Attack_Slash Swordin iskuille, Block Aegiksen torjunnalle - uusi `ICharacterVisual.Block()`-koukku kutsutaan `BlockWithAegis()`:sta): hahmon kolme FBX:ää (`Unity/Knight/Ritari_Walk_Heavy.fbx`, `_Attack_Slash.fbx`, `_Block.fbx`, kopioitu `Assets/Survivor/Characters/Ritari/Resources/`) jakavat saman 12-luun rigin, ja vain kävelymallin mesh näytetään - muiden kahden animaatioklipit poimitaan ajonaikaisesti samalle Animation-komponentille. Uusi editor-tuontiskripti `Assets/Survivor/Editor/RitariImportPostprocessor.cs` asettaa Legacy-riggauksen, kävelyn loopin (attack/block eivät looppaa) ja URP-materiaalit Blenderin väriarvoista. Testattu Unity CLI:llä Play Modessa: hahmovalinta, jatkuva miekkavahinko heti alusta (ehti tasolle 2 sekunneissa), Block-koukku laukaistu virheittä. Ei uusia konsolivirheitä.
- 2026-09-08: Velho lisätty toiseksi pelattavaksi hahmoksi Potun rinnalle. Uusi hahmonvalintaruutu ennen aloitusaseen/asekaapin valintaa (`SurvivorPottu.cs: DrawCharacterSelect, SelectCharacter, BuildCharacterVisual`); Pottu toimii kuten ennen (asekaappi, 30 asetta), Velho saa aina suoraan Salamasauvan (Lightning - ketjuttuva sähköisku) eikä näe asekaappia. Velhon visuaali on oikea Blenderissä tehty malli (`Unity/Wizard/Velho_Walk.fbx`, kopioitu `Assets/Survivor/Characters/Velho/Resources/`), ei proseduraalinen kuten Pottu. Uusi `ICharacterVisual`-rajapinta yhdistää Potun ja Velhon liikkumis-/hyökkäyskoodin (`MovePottu`, `AttackWeapons`, `CastAdvanced`) hahmoriippumattomaksi. Uusi editor-tuontiskripti (`Assets/Survivor/Editor/VelhoImportPostprocessor.cs`) asettaa FBX:n Legacy-riggauksen, animaatiolooppauksen ja URP-materiaalit automaattisesti Blenderin alkuperäisistä väriarvoista - ei vaadi käsisäätöä Unity Editorissa. Testattu Unity CLI:llä Play Modessa (molemmat hahmot, konsoli puhdas).
- 2026-09-08: Aura-efektin näkyvä koko korjattu vastaamaan oikeaa vahinkosädettä (tekstuurin läpinäkyvä reuna teki sen aiemmin liian pieneksi; kerroin 3.386→4.5). Vahvistettu skaalautuvan Size-statin mukana. Poistettu Lightning-aseen vanha keltainen "Chain lightning" -kuutiopalkki, jäljellä vain uusi Zap VFX -osumaefekti (`SurvivorArsenal.cs`, `SurvivorProgression.cs: AttackWeapons`).
- 2026-09-08: Aura-aseelle lisätty FX_magic_plane_II-paketin taikaympyrä violettina versiona (uudet materiaalit `Flipbook_Effects_Purple01-03.mat` + prefab-variantti `FX_Purple.prefab`, tintattu `_TintColor`-ominaisuudella olemassa olevista Blue-materiaaleista). Seuraa pelaajaa ja skaalautuu Auran säteen (Size-statin) mukana vanhan LineRenderer-renkaan rinnalla (`SurvivorGame.cs: auraEffectPrefab`, `SurvivorArsenal.cs: CastArsenal, TickOrbits, ClearArsenal`).
- 2026-09-08: Lightning-aseen (Salamasauva) ketjuosumiin lisätty Zap VFX -paketin sininen räjähdysefekti (`Assets/Vefects/Zap VFX URP/VFX/Zap/Particles/VFX_Zap_02_Blue.prefab`) jokaisen osuman kohdalle vanhan palkki-visualisoinnin lisäksi (`SurvivorGame.cs: lightningZapEffect`, `SurvivorProgression.cs: AttackWeapons`).
- 2026-09-08: Tavalliset viholliset ("Chaser"/"Brute") käyttävät nyt Toon_RTS-luurankomallia (`Assets/Toon_RTS/unded_Demo/prefab/UD_demo_character.prefab`) primitiivikapselin sijaan, animoituna (Idle/Walk/Attack, uusi `Assets/Survivor/Animations/UndeadEnemy.controller`). Elite-viholliset skaalattu isommaksi ja väritetty oranssiksi property-blockilla. Koko 2x/2.8x (elite), kääntyvät liikesuuntaan. Jos prefabia ei ole asetettu, putoaa takaisin vanhaan kapseliin (`SurvivorGame.cs: SpawnEnemy, Update`).
- 2026-09-08: Toteutettu koonnin loput 21 asetta (Revolver, Aegis, Bananarang, Axe, Space Noodle, Sniper Rifle, Slutty Rocket, Mines, Wireless Dagger, Frostwalker, Tornado, Dexecutioner, Blood Magic, Black Hole, Poison Flask, Katana, Dragon's Breath, Dice, Hero Sword, Corrupted Sword, Scythe). Yhteensä 30 pelattavaa prototyyppiasetta; asekaapissa 10 sivua, kaikki myös tasovalinnoissa ja arkuissa. Uudet erikoisammukset, alueet, kilpi, jäädytys, teloitussuojaus, kierroskohtainen Blood Magic -HP ja viikatteen lataus: `SurvivorAdvancedWeapons.cs`. Nykyiset 1 % Tomet, omistetun aseen bonus, päävalikko, 60-säteinen areena ja bossisäännöt säilytetty. Asekohtaiset testit ja kaikkien 30 aseen yhteistesti ajettu Unity CLI:llä; ei uusia konsolivirheitä. Mallit/efektit ovat prototyyppitasoa, pysyviä aseiden avausehtoja ei ole eikä uutta releasea julkaistu. Ohjeet: `Assets/Survivor/README-AllWeapons.md`; testit: `PrototypeTools/VerifyCompleteWeapons.cs` ja `VerifyCurrentRules.cs`.

- 2026-09-08: Geneeristen Tomejen (Damage, Cooldown, Size, Movement, Duration, Projectile Speed) prosenttibonukset pudotettu ~20 %:sta 1 %:iin per valinta, ja Precision Tome 10 prosenttiyksiköstä 1:een — kuvaustekstit päivitetty vastaamaan (`SurvivorProgression.cs: ApplyUpgrade, UpgradeDetails`).
- 2026-09-08: Vihollisen tappaessa 1 % mahdollisuus pudottaa terveyspurkki (punertava pallo), joka parantaa 25 HP kerättäessä (max HP:hen asti) — erillinen XP/kulta-pudotuksista (`SurvivorGame.cs: Hit, Drop, Update`).
- 2026-09-08: Vihollisten elämä/enimmäismäärä skaalautuu enemmän per alue (+45 % elämä, +30 kpl katto per bossin jälkeinen alue), ja XP per tappo pienenee tasojen myötä (-5 %/taso, pohja 35 %) (`SurvivorGame.cs: SpawnEnemy, GrantExperience, Update`).
- 2026-09-08: Havaittiin ettei Discordin GitHub-integraatio näytä release-tekstiä lainkaan (vain otsikkorivi) — poistettiin se repo-webhook ja siirryttiin lähettämään julkaisuilmoitus suoraan Discordin webhookiin käsin muotoillulla viestillä. Ks. "Julkaisuprosessi"-osio. Webhook-URL siirretty gitignoroituun `.secrets/discord-webhook.txt`-tiedostoon (repo on public, URL ei saa päätyä sinne).
- 2026-09-08: Kun 90s bossiportaali-ajastin nollautuu, uusien vihollisten spawni loppuu heti (olemassa olevat jäävät jäljelle tapettavaksi) — pelaaja siivoaa loput ja kävelee sitten portaalille kutsumaan bossin. XP-kerääminen estyy bossin aikana (kulta yhä kertyy). Bossin kuoltua alue vaihtuu automaattisesti ~2s kuluttua (`SurvivorGame.cs: Hit`, `SurvivorProgression.cs: TickProgression, Interact`).
- 2026-09-08: Areena kasvatettu (säde 38 → 60, pilarit/lattiamerkit/arkut/pyhäkkö/portaali skaalautuvat mukana). Kokeiltiin lisäksi kiipeäviä kukkuloita (korkeusvaihtelu), mutta ne poistettiin käyttäjän pyynnöstä — kartta jäi isommaksi mutta tasaiseksi (`SurvivorGame.cs`, `SurvivorPottu.cs`, `SurvivorProgression.cs`).
- 2026-09-08: Kauppa-, asevalinta- ja tasopäivitysvalikot piilottavat nyt taustalla olevat HUD-tekstit (yläpalkki, ohjeet, "ALUE X" / "Arkut:" -rivit) kokonaan sen sijaan että ne vain himmenivät läpinäkyvästi (`SurvivorGame.cs`, `SurvivorProgression.cs`).
- 2026-09-08: "Poistu päävalikkoon" -kesken jäävä kierros tallentaa nyt selviytymisajan (jos ennätys) ja maksaa Silver-palkkion tapoista, kuten normaali kuolema (`SurvivorGame.cs: ExitToMainMenu`).
- 2026-09-08: Jo omistetun aseen valitseminen tasopäivityksessä antaa nyt aina joko +15 % Damage tai +15 % Size (n. 35 % erillinen mahdollisuus kummallekin, taataan vähintään toinen) — ei enää koskaan "tyhjää" päivitystä (`SurvivorProgression.cs: ApplyUpgrade`, `GrantWeaponBonus`).
- 2026-09-08: Lisätty ESC-taukovalikkoon "Poistu päävalikkoon" -nappi ja päävalikkoon "LOPETA"-nappi (`SurvivorGame.cs`).
- 2026-09-08: Pystytetty GitHub-repo ([Marcebooy/SurvivorPrototype](https://github.com/Marcebooy/SurvivorPrototype), public), GitHub Releases -pohjainen build-jakelu, Discord-webhook release-ilmoituksille, sekä itsepäivittyvä launcher (`Launcher/`) kaverille jaettavaksi. Pysyvä latauslinkki: `https://github.com/Marcebooy/SurvivorPrototype/releases/download/launcher/SurvivorPrototypeLauncher.exe`.
